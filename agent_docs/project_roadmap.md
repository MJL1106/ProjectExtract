<!-- ROADMAP-CHECKLIST v1 | auto-maintained by .githooks/roadmap-update.sh — keep this line -->

# ProjectExtract — Feature Roadmap & Build Checklist

The remaining work in build order (W1 → buffer), as a tickable checklist to follow bit by bit. Everything here is *new* (not yet in game). Each item is written so a fresh chat can act on it cold — tick items as they land.

**Status key:** `- [ ]` to-do · `- [~]` in progress · `- [x]` done.
**Week tags** `(Wn)` are *soft* — order can swap, but the core sequence holds. `(stretch)` items are the first to cut under the OK/Worst scenarios below.

**How this file stays current:**
- **Primary (reliable):** whoever commits reconciles this file against the diff *before* committing, so statuses ship inside the work commit. Works regardless of which chat made the changes.
- **Backstop (automatic):** a git `post-commit` hook (`.githooks/roadmap-update.sh`) auto-reconciles from the commit via headless `claude -p`, wherever that CLI is authenticated. Never blocks or breaks a commit; if it can't run, it skips. Re-wire after a hooks reset with `sh .githooks/install.sh`.

**Milestones:** Prototype 2 ships **Tue 7/7** · polished companion demo **~20/7** · target everything done **Sun 9/8** · NY from **Fri 31/7** · sporadic buffer **10–19/8** · **hard deadline 19/8**. Dissertation draft structure due **Wed 9/7**.

---

## Scenarios (Best / OK / Worst)

Target: whole list done by end of W7 (Sun 9/8). `[Core]` ships in every scenario; `[Stretch]` flexes:

- **Best** — everything: 4-gun arsenal, attachments + ammo economy, full cover-animation rework, all breach styles, takedown variants, full UI (menu / loadout / minimap), full audio + music.
- **OK (drop less)** — keep AR + pistol + SMG, attachments, ammo economy, core cover-animation rework (skip lean/peek variants), functional menu + loadout + basic minimap. Drop/defer: shotgun, breach-style variants, class-creation polish, in-game music.
- **Worst (drop more)** — keep AR + pistol, the existing cover animations (cut the rework), one breach style, HP + ammo HUD. Normal mode becomes a don't-engage-first flag on Combat. Drop: SMG, shotgun, attachments, ammo economy, cover rework, breach/takedown variants, all UI beyond HUD, minimap, music.
- **Never cut:** one complete polished level (both halves), extraction person, HP + ammo HUD, all three companion modes, looting, objectives. (The only level cut is one polished level instead of multiple level types.)
- **Biggest flex lever:** the cover-animation rework (W3–W4) — cutting it frees ~1.5 weeks; the companion still uses cover intelligently, only the look downgrades.

---

## Timeline — week by week

### Week 22/06 (W1) — Foundations: level + companion control spine
- [~] [Core] **Marketplace level + blockout.** Buy a pre-built level (factory or apartment complex; cluttered/tight spacing) and rough-block the single polished level: a neutral no-enemy start → fight from point A → B → point of interest → extract back out. Need a stage to demo everything on.
- [~] [Core] **Companion control framework — the "ping" system.** The player marks something in the world (a "ping") — an enemy, a door, a loot spot — and the companion acts on that ping. This input layer is the spine every later companion command (takedown, breach, loot, mode-swap) plugs into. *(Follow / stay-behind movement already exists — not in scope.)* *(Loot reachability fixed 30/07: the companion silently skipped 6 of the 9 DemoMap `BP_LootContainer` caches because they sit 320-480cm above the floor navmesh (draped over furniture — intentional placement, left alone) and `MoveToActor` projects its goal with the nav data's small default extent. Candidates are now nav-projected from the container's bottom face with a tight vertical extent plus a below-reject so a stacked storey can't win, unreachable candidates are rejected at SELECTION time rather than selected-then-failed, and the arrival gate is horizontal + asymmetric-vertical so an elevated volume qualifies without letting the ally loot through a floor slab. ⚠️ `AWeaponCase` implements NO interface, so weapon cases were never `ILootable` and are excluded for free — no opt-out flag needed. `LootMontage` is unassigned on both allies, so the interact beat is currently the 2s `InterLootPause`.)*
- [x] [Core] **Pre-authored companion route tool.** A way to hand-place a path in the level that the companion follows on cue — e.g. a cinematic "companion takes the lead off the rooftop at mission start, follows the set path, then breaks off." Reused later for combat-mode lead.
- [x] [Core] **Fix player sprint-arm bug.** Holding sprint, then stopping while still holding the sprint key, leaves the player's arms animating as if sprinting while stationary.

### Week 29/06 (W2) — Prototype 2 build → deliver Tue 7/7
- [x] [Core] **Companion-synced takedown.** Player pings an enemy for a close-range takedown and the companion plays a paired/synced takedown animation alongside. *(The base player takedown already exists; the companion-side sync is the new part.)* — shoot/gun path done: ranged headshot kill synced to player gunfire (2-enemy) / autonomous 2–4s (lone), with face → aim-in → 2 shots → drop → lower presentation. Knife paired path runs via instant-kill fallback (stab anim still missing). **Commitment + body-notice round BUILT awaiting PIE (25/07):** a missed player shot used to make the companion abandon its marked victim and join the player's fight, because the global alert ratchet woke the pocket victim and the strict Unaware rule failed the task permanently — victims are now RESERVED to the companion and stay eligible through anything below Combat, and if one does reach Combat (or the shoot path's LoS is blocked) the companion latches it as a hard combat-target override and finishes it with normal gunfire before resuming normal selection (`TakedownCommitHoldSeconds` 8s safety valve on the BT node; releases on the victim's death). Commit binding is no longer method-exclusive — a KNIFE-armed companion listened only for the player's melee input, so the player's own gunshot was invisible to it; both signals now count (`OnPlayerFiredWeapon` fires before the hitscan, so the commit lands on the trigger-pull frame hit or miss). Also: the enemy stood next to a fresh takedown kill ignored the corpse entirely — sight discovery needs the body inside the head-bone view cone and a ragdoll at your feet sits below it, so a short-range out-of-cone notice now routes into the existing discovery path (bark → Searching → sweep, never Combat) after a deliberate `BodyNoticeDelaySeconds` 1s delay; reserved takedown victims are exempt so a pending companion kill still lands. The delay is load-bearing — an instant reaction is the old bug where the neighbour 180-spun and broke shoot takedowns.
- [x] [Core] **Door breach v1 (loud).** Player pings a room/door; the companion kicks it down and goes in guns-blazing. Just the loud style for P2 — the other styles come in W5. *(Breach door has a mesh + collision; companion paths to it, breaches on arrival, door swings open, companion holds ~3s then resumes follow.)*
- [ ] [Core] **Enemy model swap.** Replace the enemy character models and wire a few enemy types into the level.
- [ ] [Core] **Tune the loop.** Get 2–5 minutes of gameplay reading well; test and finalise for the Tuesday 7/7 delivery.

### Week 6/07 (W3) — Ship P2 (Tue), start cover rework, dissertation draft (Wed 9/7)
- [ ] [Core] **Deliver Prototype 2** (Tue 7/7) — release objectives in the note below.
- [ ] [Diss] **Dissertation draft structure** (due Wed 9/7). Section skeleton (intro / companion-AI design / implementation-to-date / evaluation / conclusion) pre-filled from what's built so far.
- [x] [Stretch] **Start cover-animation rework.** Shared by enemies + companion; new COD/BF-style animations — shoot over cover, shoot around cover, crouch + standing cover locomotion. *(Cover behaviour already works as a mechanic; this replaces the static-looking animations.)* *(Shipped on Cover-system-revamp: enemy CROUCH cover (Kubold montages, root-motion over-top + corner peeks, per-scenario weapon align, solid cover-wall collision, Confident-morale reseek, force-peek/force-height debug cvars); STANDING cover peeks (enemy + companion, weapon-align from tuned socket poses); companion cover-align built from scratch; cover REPOSITIONING for angles (EQS side-flag weighting + composite shuffle scoring + angle-impulse relocate); reload-gated peeks + proactive tucked reload; randomized peek/hide timings + long-hide feint; reload-in-cover torso tuck (dynamic idle-capture); move-along-wall reposition montages (Start/Loop/Stop treadmill clips, wall-facing hold, walk-speed cap); cover vision cone (no fire/track outside the peek arc — fixes fire-while-facing-away); endpoint peek-side switching (unflagged-side trace fallback) + failed-peek escalation ladder (same-wall reposition → side-switch in place → relocate); crouch stand-up-peek mix; per-scenario cover aim offsets (4 new AOs + CoverLo_AimU wired via CoverAimScenario blend in ABP, additive bases repointed); drift-correct facing fix. Enemy + companion use identical anims; peek-intelligence/AO/reposition round is enemy-only so far — companion parity pending.)* *(Playtest bug-fix rounds: failed-peek ladder rebuilt as a deterministic per-cover stage machine (side fail x2 → over-top → swap side → over-top → relocate, DA-tunable thresholds); repositioning/shuffle no longer twists mid-move or leaves the wall (perpendicular same-wall gate, engine Move-focus conflict fixed, arrival re-faces both stances); fire cone and torso-tracking reach unified to one value so looking at the player and being allowed to fire always agree; through-wall aim-tracking stare killed via a LOS flag; instant cover-break when flanked with a clear angle and LOS (compromise + LOS, ForceCover pin respected); endpoint cover positions corner-aligned via a wall-edge trace so capsule standoff no longer varies by bake spacing; post-peek idle/over-top side now follows the side of the peek that actually played instead of a rewritable lean flag; grenadier grenade lob moved in-task — the BT lob branch aborted cover combat on every tuck (twisted throws over plain legs, dropped cover pose after peeks, starved reloads), now accumulates in-task and throws from the tucked pose with wind-up holds + cancel-on-move.)* *(Cover-intelligence round: unified enemy cover scoring — one shared scorer for the EQS and C++ pick paths, all weights data-driven in the archetype DA; honest last-known-position cover decisions (no through-wall psychic picks — flanking a blind enemy works); approach-path exposure penalty (won't sprint across your line of fire); corner + retreat-viability scoring terms; geometry-first idle-lean/peek-side at wall ends (fixes wrong-way idle + constant over-top stand peeks); per-enemy Press/FallBack posture that advances to closer cover when unpressured and falls back when broken — Rusher/Grunt/Grenadier/Pistol/Heavy on, Sniper hold-only, Officer/Shotgun off.)* *(Companion-coexistence round: enemy multi-threat cover awareness — picks AND held covers scored/invalidated against every known hostile (sighted or perceived within a memory window at frozen last-known position, honest knowledge; aggro-switching to the companion no longer forgets the player); hostile claim guards — EQS intent/proximity filter + occupancy checks at pick/mid-move/arrival so enemies never take or share the companion's cover (plugin OccupyCover fails silently — races now tie-break closer-agent-wins); stale-cover-state fix — BB_HasCover validated against actual position every tick, Combat→Searching teardown fully clears pose/crouch/BB (no more "taking cover" in the open while searching or pushing up); Heavy + Sniper BTs were missing the Keep-Cover-Occupied service entirely — wired.)* *(Proactive grenadier lob: grenadiers now roll a per-cover-pause chance to lob a grenade over the top while engaging from crouch cover — not only when the player hides — split ~65% tucked (crouch montage) / ~35% pop-up-over-the-wall (stand, throw, duck back), both DA-tunable and throttled by the existing cooldown/supply; orphaned BTTask_GrenadierLob dead code removed.)*
- [~] [Core] **Enemy AI-director review + test.** The existing enemy director needs a review/tuning/test pass. *(Spawn-quality round shipped + playtested: floor-embedded spawns fixed (capsule half-height above nav floor), zone sightline check moved to head height per squad member with spawn-fallback parity (no more squads materialising in view), scored zone selection replacing random pick — distance-band preference + companion-proximity penalty + jitter, min distance to zone-box closest point; sniper composition cut from both Room 2 director configs; heavy headshot fraction 0.65→0.25 (4-tap not 2-tap); stim use keybound (3, was unmapped).)* *(Wave-pacing round BUILT awaiting PIE (25/07): the wave's first squad no longer waits a full cadence — `StartWave` zeroes the spawn timer and the cadence gate applied to squad one too, so a 25s cadence opened the defence with 25s of silence; new `FDirectorWaveRequest::FirstSquadDelaySeconds` (default 2s) gates the opener separately. Guaranteed compositions added — `FDirectorGuaranteedSquad` (1-based squad number + composition name) pins a set-piece squad to a slot, bypassing BOTH the weighted roll and the MaxAlive fit filter, because composition choice was pure weighted RNG and the Heavy only ever appeared on a lucky roll; an unresolvable name warns and falls back to the roll rather than stalling the wave. `ShouldSpawn` and `PickComposition` share one "is a guaranteed squad next" helper so the alive-cap waiver can't disagree between them. Level values AUTHORED + saved + verified from disk: `DA_DirectorConfig` `ObjectiveConfig` `MaxAlive` 12→20, and the DemoMap flow's `ExtractionWave` — `TargetSquads` 4→6, `SpawnCadenceOverride` 8s, `FirstSquadDelaySeconds` 2s, Heavies guaranteed on squads 3 and 6. ⚠️ **The wave runs on `ObjectiveConfig`, which contained NO Heavy composition at all** — `HeavyPush` existed only in `ExtractionConfig`, so the "random Heavy" everyone assumed could happen was impossible; the comp value was copied across into `ObjectiveConfig` (the append-whole-comp-value lever, since the struct fields are `EditDefaultsOnly`). `ObjectiveConfig` also carries a duplicated `Flush` entry (listed twice, so it rolls at double weight) — left as authored, worth a look. Remaining: tension tuning (tension is bypassed entirely while a wave is live, so cadence + MaxAlive are the only wave levers); ShotgunPush composition has no shotgunner class; no difficulty scaler exists.)* *(Post-wave 4-bug round BUILT awaiting PIE (25/07): last-man rally — stuck wave survivors (morale-gated pursuit meant a Shaken last enemy never left its spawn room, blocking CompleteWave forever) get ForceEngage + a new RallyToConfident every 12s of stale combat once all squads are spawned; blocked-wave relief — a wave blocked ≥2× BlockedWarningSeconds waives the spawn sightline gate so it can't soft-lock the exit; heavy LMG blind-fire fixed — BTTask_HeavySuppress now fires only with real LOS (FireLosLostGrace continue-grace), a ForceEngage'd never-sighted heavy advances silently instead of hosing walls; spawn-zone anti-repeat + per-zone bias — RepeatZonePenalty 30 now bites only after 2 consecutive picks, new `WaveScoreBias` EditAnywhere on AEnemySpawnZone (wave picks only, ambient unaffected), East/hallway zone set +20 in DemoMap → ~2:1 East-major EEN cadence (user call: hallway approach is the harder/better one), never 3+ in a row; companion/extractee wave-hold hardened — chokepoint overwatch aim persists for the whole hold (no 6s-cap blank stare), wave-hold added to the facing-tier yield, and a 20s stale-combat release (WaveHoldStaleCombatReleaseSeconds, suppressed while a Combat enemy is within the 2500 leash) so a stuck wave can never freeze the allies at cover indefinitely.)* *(Round 2 same day, PIE feedback: wall-fire sweep — the heavy fix held but SIX more fire paths shot without LOS (EnemySuppressFire fired instantly at through-wall spawn LastKnown; BoundingAdvance/Flank/RusherAdvance/CircleStrafe fired unconditionally during maneuvers; MoveAndShoot unbounded in pursue mode incl. Shotgun's bMoveAndShootPursue; CombatFire cover-transit bursts outlived LOS) — all now gated on BB_HasLineOfSight with FireLosLostGrace hysteresis, suppressed blind-fire additionally requires ever-having-seen-the-target (bEverHadEffectiveLOS); extractee frozen-between-squads fixed — wave-hold overwatch now runs even when posture decayed out of the combat branch, with release-edge lower/focus-clear, and ComputeOverwatchAimPoint falls back to the director's wave spawn zone (new GetWaveThreatReference) when fight memory is empty, so both allies aim at the door squads actually come through.; last-enemy-hides fixed for good: deterministic membership-floor latch in ReassertWaveMemberEngagement -- when all squads are spawned and the wave's own alive roster is at/below LastManHuntThreshold, survivors latch immediately, independent of scope volumes (runner outside scope read as 0 alive and never armed) and of the 12s stale-combat rally (starved by Combat re-entry re-stamping LastCombatReportTime).)*
- [~] [Core] **Companion locomotion / grip polish.** Tidy the companion's gun grip and stance by reusing the enemy animation setup. *(Extractee pistol drift during cover peeks, 30/07 — ROOT CAUSE FOUND, fix is designer-side and OUTSTANDING. `ABP_ExtracteeMain` was duplicated from `ABP_CompanionMain` and all eight `CoverAlign*Transform` class defaults plus `FireAlignSocketName` are still the RIFLE's values, applied to a pistol; out of cover the scenario is `None` so nothing is offset and it looks fine at rest, while the peek/over-top offsets carry the largest reach and make the same proportional error obvious. The remaining fix is retuning those Class Defaults against the pistol in hand — iterative nudge-and-observe in PIE. ⚠️ The "`bInCover` collapses mid-peek" theory is FALSE and cost 3 earlier rounds: both ABPs already OR `bInCover` with `IsAnyCoverPeekMontagePlaying()`. The captive-kneel-baseline theory is ALSO false — the bake's only pose-dependent term cancels because the weapon socket is parented to the same `hand_r` used as the align reference, so it is pose-invariant. Diagnose with `companion.AlignDebug 1`: `[ALIGN]` names the winning writer per frame (fire → patrol → cover, absolute writes so LAST wins, meaning cover-align owns every peek frame), `[ALIGN-BAKE]` prints `socketToBone`. C++ shipped: a rest-pose restore that fixes a real latent bug where any later bake composed on top of the previous bake's output.)*

> **P2 min-spec (release objectives, Tue 7/7):** first half of a level · 2–5 min gameplay · few enemy types · 1 weapon, no attachments · takedown sync with companion · companion cinematic route at level start · basic player control over companion · ping a room for the companion to open · enemy model changes.
> **P2 priority extras (if time, in order):** cover rework · AI director · second half of level · companion locomotion · enemy weapon attachments.
> **P2 definite-no:** multiple weapons · UI/UX · stealth & combat modes · VFX/sound · extraction person.

### Week 13/07 (W4) — Cover finish + companion combat brain
- [~] [Stretch] **Finish cover rework.** Peek/lean round corners while aiming, move-and-shoot *around* cover (not glued to it), and hold-the-angle logic: when high-HP and not under pressure the companion holds the last-seen enemy angle instead of ducking into cover. It should prefer cover but never stop firing mid-fight just to reach it. *(Peek/lean-round-corners + move-and-shoot / cover-to-cover repositioning shipped on Cover-system-revamp. Remaining: hold-the-angle logic — high-HP, low-pressure companion holds the last-seen angle instead of ducking to cover.)* *(Companion cover-intelligence parity + churn-polish rounds shipped: killed the blind-cover ping-pong (EQS hands out covers with no LoS to the enemy) and the discarded-monitor-swap stomp; peek cycles now persist per-physical-cover so relocate gates stop starving; all companion cover LoS tests trace to the enemy's HEAD not centre-mass (fixes the 27s hold-never-shoot vs a standing shooter behind crouch cover); speculative "peek for peeking's sake" like the enemy; blind-hold RELEASE ladder — when a cover can't fight the target (empty peeks / long blind hold / enemy pressuring the player) it vacates to open-engage and move-shoots toward the fight; hostile-adjacency cover rejection (won't set up next to an enemy); cover-to-cover ADVANCE in all 3 modes (free in Combat, player-movement-gated in Normal/Stealth); corner-peek apex trace-marched off the real wall corner (no more short peeks); companion fire now suppresses enemies (weighted below the player); 0.5s peek-fire delay so shots start after the animation reaches exposure; fixed a BT deadlock where a spontaneous FinishLatentTask(Aborted) froze the whole tree. Remaining: hold-the-angle logic; Combat-mode move-shoot/cover blend (cover currently force-taken).)* *(Crouch-peek wallhack round, playtested: peek-type choice at crouch cover is trace-scored before exposing — corner left/right at the true lean frame vs over-the-top, corner-biased ties, multi-threat exposure penalty, score-weighted roll so corner crouch peeks always stay possible; Normal mode is pressure-responsive — peek cadence compresses, Hold decays and bursts lengthen as enemies close, plus a peek-now impulse when a closing threat crosses ~9m; a point-blank visible threat abandons cover logic entirely (in-cover release + commit decline) killing the chased-in-circles loop; the companion now fires at the visible target while walking to committed cover instead of running silently; burst clock starts at exposure, not peek trigger — quick peeks no longer end before the first shot.)* *(Companion flank-break parity (enemy compromise-break port): the cover switch monitor now hard-breaks a held point that stops protecting — focused enemy past the widened fire arc, failed body-shield trace, or a known second threat with the body exposed — on its own fast cadence (0.4s eval, 2-eval debounce), bypassing the dwell/margin/peek-cycle/agree gates to relocate to a protecting candidate or vacate to open-engage when none qualifies; a HIT from an enemy the point doesn't shield against breaks instantly (no debounce) and that shooter steals target focus past distance-stickiness so the companion turns and fires on the flanker; churn-guarded by a per-break cooldown on the geometric path + freshness gate on the hit path + the purposeful-commit grant so the relocate survives MoveToCoverPoint's commit gate.)* *(Second-fight churn fix, playtested: combat task rejects a claimed cover whose hunker is >450uu away at entry (FINALAPPROACH-REJECT — releases + MarkVacated for re-pick instead of crouch-trekking across the room); the switch monitor re-earns its arrival latch when the BB cover handle changes and holds ALL switch/compromise decisions while the pawn isn't physically at its current point (killed mid-trek CoverSwitch/CompromiseBreak churn); plus [CombatMove] diagnostics tagging every combat mover (jiggle-drift/angle-seek, regain-los fan direction, player-pull, cover-move) with backpedal dot + player distance.)*
- [~] [Core] **Companion Combat mode.** Aggressive; takes the lead; moves cover-to-cover prioritising forward progress; gains space without suicidally rushing in; likely needs its own combat "director" like the enemies. Companion's gun is predefined per level with infinite ammo. *(Involvement round SHIPPED + user-approved "feels so much better" 28/07: burst/pause now mode-scaled — `CombatBurstDurationMultiplier` 1.35→1.6 with `ModePauseScale` as its reciprocal, so Combat runs 1.44-3.2s bursts against a 0.625s pause vs Defensive's 0.9-2.0s against 1.0s (live `FirePauseDuration` on the BT node is 1.0, NOT the 0.5 C++ default — any cadence maths must use the asset value). One free repositioning bound per confirmed kill via `TryConsumePostKillHopBypass` + `CombatPostKillAdvanceWindow` 3s, stamp consumed on BOTH exits so a kill can never chain two bounds; hop leash is `FMath::Min(CombatLeashDistance, CombatAdvanceHopMaxPlayerDistance 1400)` so the new lever can only tighten. `CombatAdvanceHopInterval` 5.0→3.5, `CombatAdvanceScoreMargin` 1.6→1.35, `CombatModeLeadDistance` 400→550. Post-fight weapon-up hold (`CombatWeaponUpHoldSeconds` 2.5 — bounded ABOVE by `ExploreReturnDelay` 3.0, since the anim raise clause needs `Posture == Combat`) arms a pawn-anchored forward focal rather than clearing focus, because `CompanionAnimInstance.cpp:245` is the only consumer of `IsLowReadyAim()` and needs a live focal to raise at all. Remaining: no combat director, no difficulty scaler.)* *(Eyes-on + few-enemy mobility round 30/07, BUILT awaiting PIE: every cover commit now validates a real peek line to the combat target (`ValidateAndRerankCover` — CanPeekShoot on the EQS winner, nearest-first re-rank capped at `CoverEyesOnMaxCandidateTraces` 12, no-candidate → decline+MarkVacated → open-engage; fixes the single-enemy blind-corner hide, which only the multi-threat re-rank ever guarded and it early-outed with <2 threats); few-enemy Combat (<`CombatOutnumberedCount` known threats) pairs the low-HP clause with live incoming fire on BOTH commit and release-hold (`IsFewEnemyCombat`/`HasWoundedFirePairing`, spike-bar-equal so no duck-cycle; kills the wounded camp where static low-HP in `IsStrongPressure` blocked the 4s natural release until regen crossed 50%); LostContact bark suppressed when the eye-trace blocker impact is within `SelfOcclusionBarkSuppressRadius` 250 (no more "where did he go" at its own wall); speculative no-LOS peek bursts hold aim down the fire arc until the muzzle gate first clears (`bBurstCommitSpeculative`/`bSpeculativeAimVerified` — no through-wall tracking; aim re-asserted at the verify sites so the first shot is aimed). ≥4-enemy fights + Normal/Stealth byte-identical by construction. OUTSTANDING content task: companion archetype's `CoverPeekOverTopMontage` unwired (`bWarnedOverTopUnwired`) — the "stands with no cover pose" look.)*
- [~] [Core] **Companion Normal mode → "Defensive".** Doesn't engage first — only engages once the player is spotted/shot at, with an instant reaction; confident but never moving ahead of the player. Sits one level above stealth. *(SHIPPED 28/07: relabelled **Defensive** — `UMETA(DisplayName)` + the two WBP `NormalLabel` NSLOCTEXT defaults only; the enumerator stays `ECompanionMode::Normal` so no Blueprint re-picks. The unaware-enemy gate is KEPT by user decision (no kill floor — a perfect stealth run legitimately yields zero companion kills), but widened: new `CanAcquireTarget(Mode, bEnemyDetectedPlayer, bEnemyAwareOfCompanion)` beside the unchanged `CanEngageUnawareEnemy`, backed by `AEnemyCharacter::HasEngagedCompanion` → `UEnemyAwarenessComponent::HasLiveKnowledgeOf`. ⚠️ The old premise "an enemy shooting the companion is invisible" was WRONG — `HasDetectedPlayer()` returns `AwarenessState == Combat` and is target-agnostic, so that case already passed; the real gap was **Searching/Suspicious** enemies (heard gunfire, found a body, shot at and missed). Note `IsCompanionSightCloaked` still hides a Defensive companion from any enemy not already in Combat, so pre-alert nothing escalates off it — the CLOAK, not the gate, is the lever if that ever needs changing. First-peek wait halved (`FirstPeekWaitScale` 0.5, Stealth explicitly excluded); `MaxConsecutiveHolds` 2→1 with `StealthMaxConsecutiveHolds` 2 preserving Stealth. Stealth verified byte-for-byte unchanged path-by-path.)* **Damage + peek-responsiveness buff SHIPPED 30/07 (user-verified in PIE: "feels way better"):** two root causes, both hidden. (1) The AI damage-mitigation gate in `AWeaponBase::FireHitscan` applies to the COMPANION too (`UDamageMitigationSettings` docstring says so explicitly) — `PerVictimDamageInterval` 0.35 capped him at ~3 damaging hits/sec regardless of his 10 r/s fire rate, and `RampResetGapSeconds` 0.60 was shorter than his own peek gap (1.6-2.2s cooldown + 0.5s `PeekFireDelaySeconds`), so the 0.30→0.95 accuracy ramp **reset on every single peek and never once reached settled in normal play**. Fixed data-only on `DA_DamageMitigation_Companion` (his OWN asset — enemies use `/Game/Core/Weapons/Data/DA_DamageMitigation`, untouched): Acquire 0.30→0.55, RampTime 0.80→0.60, RampResetGap 0.60→**2.0**, PerVictimDamageInterval 0.35→0.15. ≈2.4x damage per peek. Aim cone deliberately LEFT at 8°/1.5° by user call. (2) `Pressure01` — which shortens peek cooldowns, stretches bursts and collapses the hold weight — was computed from **distance to nearest threat ONLY**, so a firefight at 18m read as fully calm and the aggression response never engaged; the low-health table (Hold 50, cooldown ×2.0) then won unopposed = turtling. C++ fix: `Pressure01 = max(distanceTerm, fireTerm)` where fireTerm = `max(supp01 × PressureSuppressionWeight, recentHits / PressureDamageHitsForMax)`, reusing the existing `CoverCommitUnderFireWindow`; two new tuning fields (1.0 / 3). ⚠️ Do NOT gate the fire term on `bIgnoreSuppressionInCover` — that flag stops suppression BLOCKING peeks; this is the opposite intent. Data: PressureFarDistance 2000→3500, CooldownScaleAtMax 0.5→0.4, HoldWeightScaleAtMax 0.25→0.15, BurstDurationMultiplierAtMax 1.3→1.6, PeekImpulseDistance 900→1400, DefensivePeekCooldown 0.8→0.65, DefensiveBurstDuration 1.0→1.35; BT node `MinCoverIdleDwell` 1.2→0.6, `LowHealthCooldownMultiplier` 2.0→1.4. New `enemy.DrawDistances` cvar draws P/C distances above enemies + `p01` above the companion (greys to "(stale)" outside the cover branch — `Pressure01` is cover-branch-only by design). ⚠️ Note `IntensityCeiling` is inert wherever `bSustainedPressure` is on. Pending: PIE pass on the fire-term half specifically (enemies suppressing from range while stationary — the one case the data-only tuning could not reach).*

### Week 20/07 (W5) — Polished companion vertical-slice demo (the dissertation demo)
- [~] [Core] **FPS performance baseline and measurement gates.** Baseline instrumentation is in flight; runtime optimizations remain measurement-gated.
- [ ] [Core] **Companion Stealth mode.** Crouched/slow movement behind the player, no sprinting, acts on the ping system, whisper voice lines. Plus stealth rules: not seen by enemies, the companion's gunfire and footsteps aren't picked up by enemy AI, and it instantly breaks to combat if the player is spotted.
- [~] [Core] **Mode-swap + AI director.** Player can switch the companion's mode; missions suggest a mode (e.g. stealth) but the player can ignore it, and the AI director scales difficulty so going guns-blazing on a stealth mission isn't overpowered. **Mode-swap done:** X opens a numbered picker (1 Stealth / 2 Normal / 3 Combat) that morphs the HUD chip into a list; the number keys select the mode and are consumed while the picker is open so they take precedence over weapon switching. Pending: AI director (mission-suggested mode + difficulty scaling), playtest. **Extraction throughput fix 30/07:** the companion damage buff made the EXTRACTION beat far too intense — not because the director got angrier, but because its only brake was `MaxAlive`. `ExtractionConfig` is the one phase config with `bSustainedPressure=true` (tension sawtooth off, no Peak/Relief lulls at all), so it paces on cadence + MaxAlive alone; slow clearing used to keep alive-count pinned at 20 and most 9s beats were skipped, and faster kills turned that cap from a brake into a conveyor. `ExtractionConfig.ConcurrentSpawnZones` 2→1 (halves per-beat volume, kills the two-direction pincer). ⚠️ **Design smell worth revisiting:** `OS_Defend.defend_seconds` is a flat **150s** timer and `OS_KillEnemies` is decorative HUD only — completion is NOT kill-gated, so killing efficiently just summons more enemies for the same duration. Also note scripted waves (e.g. `Room2Defence`, 5 squads / 8s `SpawnCadenceOverride`) ALWAYS pace on cadence+MaxAlive regardless of `bSustainedPressure` — a live wave owns the director and the whole ambient/tension gate is skipped. Next levers if still hot: `SpawnCadenceSeconds` 9, `MaxAlive` 20, or trimming the 9 hand-placed bay enemies.
- [~] [Core] **Companion looting.** Player pings a lootable spot (cupboard, table…); the companion goes and loots it, then moves to the next if there's more. Loot goes into a shared objective inventory — e.g. a keycard you "use" later, not a hands-on item. — C++ done (ILootable + ALootContainer, ping→ConfirmLoot→BTTask_CompanionLoot anchored auto-sweep, UMissionInventorySubsystem grants ammo/keycards to the player, keycard-locked BreachableDoor via Interact, player direct-loot on Interact). **Volume-migration regression FIXED 28/07:** since loot moved from mesh-backed crates to meshless `ALootContainer` volumes draped over furniture, the companion looted nothing — both lootable LoS probes trace to `GetComponentsBoundingBox().GetCenter()`, which now sits INSIDE the drawer/cabinet the volume wraps, and that actor is not in the ignore list (a crate WAS the occluder and was ignored, so the trace was always clean). Measured live: 6 of 9 placed containers in DemoMap failed. Fix accepts a blocking hit within `LootOcclusionTolerance` of the box SURFACE via `Bounds.ComputeSquaredDistanceToPoint` — NOT centre-plus-diagonal, which gave a 126.6 cm omnidirectional radius and false-accepted the tightest wall on 5 of 9 containers. **40 cm is measured and bounded ABOVE:** farthest draped-furniture hit 53.9 cm vs tightest structural wall 43.1 cm (`BP_Floor_One_Walls1`) — the ranges overlap, so no value clears both; 40 is the largest with zero wall false-accepts and 96.5% furniture acceptance. ⚠️ Rotating a container eats the 3.1 cm margin (a 45° yaw inflates the AABB half-extent from 50 to 70.7). `ExploreViewerHasLoS` deliberately left byte-identical — it also gates the ENEMY scan, so a separate `ExploreLootableHasLoS` was added instead. `PE_R1_LootCache_A/B` are 2074-2205 uu from their only same-floor door, past `ExploreLootRadius` 1200 — level-design call, do NOT raise the radius (it reaches into neighbouring rooms, the bug the LoS gate exists to prevent). Pending: BT Loot branch + WBP/DA wiring + loot montage + playtest.
- [x] [Core] **Objectives + distance marker.** A simple objective system (collect item X / explore room Y / extract person to location Z) with a visible on-screen distance marker showing where to go. — Done + playtested: world-space billboard markers (AObjectiveMarkerDisplay + WBP_ObjectiveMarker3D — no camera-bob, bounds-centre + offset anchoring, through-wall AfterMotionBlur translucent material, distance-scaled, HDR-tinted white text with outline), old screen-space widget retained as off-screen edge indicator.
- [~] [Stretch] **Breach styles (tactical + quiet)** on top of loud. *Tactical:* kick the door, then fall back behind the wall connecting to it and play it cautious before entering. *Quiet:* silently unlock the handle and open the door. Player picks the style. — C++ + wiring done and playtested (EBreachType re-derived from the companion's LIVE mode at montage start — a mode switch mid-approach wins, BB write is just the fallback; per-type retargeted montages on BP_Companion; computed door stand-point + align phase; door swings at per-type contact delay from tuning DA; per-type hearing noise; combat mid-approach breaks the breach off for re-ping; post-breach the companion clears the doorway — Loud walks in past the swung leaf and sidesteps, Tactical/Quiet sidestep outside beside the frame; per-door `bForcePushThrough` override makes Tactical/Quiet also enter the room, movement-only — montage/noise stay mode-appropriate; enter points nav-projected same-floor-first so stairwell doors work; after a push-through the companion waits inside for the player, abandoning if the player leaves ~2x follow distance from the door). Pending: tactical fall-back-and-clear entry behaviour (a distinct "kick, retreat behind wall, pause" beat — current v1 kicks straight through).
- [~] [Stretch] **Takedown variants.** Hand-to-hand vs gun takedown: no suppressor forces a hand-to-hand takedown; on a gun takedown the companion one-taps the head, and if the player misses their shot the companion assists with a headshot after ~1.5s. — gun takedown (companion head-tap) + headshot damage (65% of max HP, enemies-only, shield-first) done. Companion knife-takedown polish shipped: victim reaction montage held on its last frame + killed mid-collapse (KnifeTakedownKillAtSeconds 2.15, ragdoll inherits the fall) + corpse depenetration cap — killed the corpse-pop after a companion takedown; radial CommandedTakedownBackpad lever (push straight back along the authored approach without rotating the placement); takedown-pocket enemies exempted from the close-range detection buff (NearFillBoost + point-blank auto-combat) so sneak-ups work again; a takedown-frozen victim no longer barks "man down". Pending: suppressor-gated hand-to-hand-vs-gun selection + the player-miss → companion-assist timing.
- [x] [Core] **Enemies use doors + door-aware hearing.** Enemies (and the companion) auto-open any unlocked door their path crosses (ADoorBase doorway trigger; UWC pack doors reparented to AScriptedDoor with their BP timelines driven via OnBreachRequested/NotifyDoorStateChanged; companion breach suppresses the trigger while it owns the door). Enemy hearing is acoustically occluded (AIAcoustics): walls/floors/locked doors fully block noise, closed openable doors pass it muffled (0.6x, DA-tunable), open doors don't attenuate — trace + door-portal fallback, no designer volumes. Playtested: enemies hear shots through doors, walk over, open, investigate.
- [~] [Core] **Light game-feel polish.** Muzzle flash + core weapon sounds so the demo reads well; capture demo footage. — Muzzle flash done (pooled Niagara per weapon, KINEMATION pack flashes, verified + rotation-corrected sockets on all rendered AI/companion gun meshes). Player first-person muzzle flash done + playtested (SetFirstPersonMuzzle anchor on the kit FP gun's Muzzle component, owner-local flash; DA_AssaultRifle/Suppressed FX assigned). Player dry-press auto-reload done + playtested (bAutoReloadOnEmpty was False on BP_AssaultRifle/BP_Rifle CDOs — player fire is pure C++, kit BP fire chain is dead code). Bullet tracers done (Lyra NS_WeaponFire_Tracer driven per shot from the shared fire-FX hook, all AI weapons). Core weapon sounds BUILT awaiting PIE — all C++/DA-driven since the kit BP fire chain is dead code for the player: DA FireSound/SuppressedFireSound/DryFireSound played from Multicast_PlayFireFX at the muzzle (suppressed report picked via the FP muzzle-slot mesh check OR IsSuppressedEffective; dry click once per press in the KitFire dry branch), plus DA ReloadSound/ReloadEmptySound foley played attached in Reload() and stopped on cancel/EndPlay. Assets (user-directed all-KINEMATION for fire/suppressed, editor-previewed + approved): AR TR15 fire + V1 suppressed / SMG AK105 fire + suppressed / pistol WK-11 Viper fire + TR15-V4 suppressed / sniper SRM-12 shot-with-bolt + Herrington suppressed + SRM-12 reloads; AR/SMG/pistol reload = 6 timed kit-foley SoundCues in /Game/Core/Weapons/Audio at 0.6x with t=0 grab layer (mixer InputVolume defaults to 0 per input — must set to 1s); per-calibre kit dry-trigger waves. Pending: final sound PIE pass, demo footage capture.

Search/Breach room exposure C++ landed (room-scoped AI sight, noisy/Quiet startle parity, same-room Loot preservation). First PIE pass found and fixed mode-policy gaps: Normal/Stealth now preserve no-first-shot while continuing Loot and watching threats; Tactical breach audibility covers the exposed room. Focused retest pending; the broader Breach, Stealth, and Loot items remain open.

### Week 27/07 (W6) — Arsenal + extraction person + 2nd half (home Mon–Thu, NY from Fri 31/7)
- [~] [Core] **Second half of level.** Finish the back half: point of interest → extract back out (to the start, or a separate extraction point). — Room 2 gameplay spine LANDED (branch Cover-system-revamp): Stealth-gated BP_Double_Door7 entrance, stealth-discipline punishment volume, extraction-target NPC actor starting a finite adaptive Director wave, wave-gated lift interaction, Level Complete + restart screen; all wired in DemoMap floor 2, awaiting playtest. Final-wave fight fixes shipped: heavy fire-on-the-move advance (pushes to EngageRangeMin instead of anchoring at spawn), doorway anti-stack (enemies inside a door's trigger mutually move-ignore so a funnel squeezes through instead of piling on the lead pawn), and an out-of-order objective fix (looting the keycard or unlocking the exit door before the step is current now catches up). Pending: purchased NPC mesh, level art/cover pass, zone/volume tuning.
- [~] [Core] **Extraction person.** PIVOT (approved): armed rescued VIP = second companion (`AExtracteeCompanion : ACompanionCharacter`) — captive until Rescue interact, then fights with scoped pistol, DBNO/revive like the companion, no player commands; rescue triggers the Director defence wave. C++ built + BP_ExtracteeCompanion wired in DemoMap. Fight-aware follow landed (live-fight signal → watch-stance fallback + LoS-biased follow slots) so a trailing no-eye-line companion reads the fight instead of strolling. VO LANDED (24/07): own voice "Extractee VIP" (ex-military read — the panicked-civilian take was rejected as not military enough), own `DA_Barks_Extractee` so he no longer speaks in the primary companion's voice, 9 bark types / 15 takes, plus the rescue two-hander (companion pistol-handoff line + VIP reply, `ReplyLineDelay` 2.8→5.6 so the reply stops truncating the handoff). Remaining: PIE verify (incl. fight-aware follow + door-cover fix); `Extractee_Scripted_ExtractionArrival_01` generated but undeliverable until `ABarkTriggerVolume` can target a non-primary speaker. Old unarmed `AExtracteeCharacter` kept for future missions (no VO hookup at all); scared-anim work parked. **Rescue beat re-sequenced + level re-wired (25/07):** the rescue is a paced three-stage beat instead of one instant — hold E for 5s to free (`RescueHoldSeconds`; the companion's opening line runs *under* the hold), freed UNARMED with the pistol hidden and combat refused (`StartWeaponFire`/`CanFire` overridden), then armed as the handoff line ends (`SpeakScriptedLine` now returns its duration so the beat chains off the line instead of a fixed clock; `ReplyLineDelay` 5.6→0), and `OnRescued` — the wave trigger — moved from freed to armed + `WaveDelayAfterArmed` 1s. Damage immunity extended through the unarmed window (the team flips to player-side at freed, so enemies would otherwise get a free unarmed target); `bCaptive`/`bArmed` replicated. Generic hold-to-interact added to `IWorldInteractable` (`GetWorldInteractHoldSeconds` + `OnWorldInteractHoldStarted`; 0 = instant, so every existing interactable is unchanged), player-side hold mirrors the revive hold, and `URevivePromptWidget` extended to serve interactable prompts — lighting up `GetWorldInteractionPrompt`, which had never been displayed anywhere. **Flow no longer requires the deleted placeholder:** `AExtractionTargetActor` is gone from DemoMap (the VIP owns the beat now), which was hard-failing `ValidateReferences` and killing EVERY objective in the level; the flow accepts either owner and runs the defence wave itself (`ExtractionWave`/`ExtractionCompletionAction`, direct Director binding, UnlockExit via its own LiftGate). Also: door-event late-entry catch-up generalised to every door step (a dropped `Room2DoorOpened` deadlocked the flow with no recovery), marker falls back to a stable anchor instead of going blank, Defend marker pinned to the rescue spot instead of trailing the VIP, wiring audit warns on unset instance refs. Director diagnostics: `bLoggedNoZone`/`bLoggedNoComposition` now reset on wave start (they only reset after a *successful* spawn, so a wave could spawn nothing for its entire life and log nothing at all) and zone rejection prints a per-filter tally. **Level wired:** flow `Extractee` → the VIP, `ExtractionWave` = `Room2Defence` / 4 squads / `DA_DirectorConfig`. Waves now spawn (user-confirmed in PIE) — the remaining complaint was pacing, addressed by the wave-pacing round on the director line above. **Cover pistol grip fixed (25/07, ABP-only):** cover idle + all peeks now keep the WK-11 pistol hold — dual clavicle grip layers (mesh-space standing / local-space cover) + hands layer over the rifle-authored cover anims, cover-aim layer excludes the arm branches so the AO aims the torso only, rifle-tuned CoverAlign transforms zeroed for the pistol; peek gating keys on (bInCover OR peek montage) because peek-fire calls ExitCoverPose and clears bInCover for the whole window. Remaining: no unarmed locomotion state in `ABP_ExtracteeMain`, so the VIP mimes an invisible pistol through the handoff window; the opening line ("let's get you out of here") does not exist in the repo or in git history on any branch. **Leader-chain follow + warp hardening committed 28/07 (build/PIE state unverified at commit time — checkpointed from an uncommitted working tree, not authored in that session):** `UBTTask_FollowPlayer` resolves a leader per tick (player for the primary, primary companion for the VIP) with `bSecondaryFollowsPrimaryCompanion` / `SecondaryLeaderLeashDistance` 2000 + 300 re-acquire hysteresis, a 2 s cached-leader rescan, leader-handover latch reset, Z-transit envelope so a stairwell leader does not read as "close" through a floor, and mirrored formation offsets that centre the VIP back on the player's track; `ACompanionAIController::TeleportToLocation` gained a non-mutating `FindTeleportSpot` probe, one-warp-per-tick, refusal while traversing (`StartTraversal` drops the capsule to NoCollision so the overlap probe reports success vacuously), and a latched single-shout warning; overwatch aim now re-resolves on `OverwatchAimRefreshInterval` 1 s instead of once on entry; wave hold gained `WaveHoldQuietRearmSeconds` 3, `WaveHoldMaxBlindHoldSeconds` 45 and a per-wave ceiling latch. **"VIP feels stupid" round SHIPPED 29/07 — the cause was NOT AI logic.** An audit confirmed the VIP runs the same BT, controller class, tuning asset, service and combat task as the primary with byte-identical stats (engage range, accuracy, health) — ruled out and not to be re-investigated: BT node state sharing (every stateful companion node is `bCreateNodeInstance` or NodeMemory), pawn CDOs (full reflection diff — only `AutoPossessAI`/`ModeWidgetClass`/`WeaponClass`/`WeaponAttachSocket`/`BarkSet` differ, all deliberate), runtime movement/rotation, perception (lives on the shared controller), enemy-side treatment, aim clamps, and mode-edge latches. ⚠️ The audit ALSO flagged `DA_Barks_Extractee`'s 9-of-45 categories and unset `Line` fields as gaps — **that was WRONG**: 9 bark types / 15 takes is the authored scope (see this item's VO round) and `Line` is a retired subtitle field. Subtitles are not used and will not be. Do not re-raise. Real causes fixed, three asset-side: `DA_ExtracteePistol.EnemyAnimSet.WeaponFire` pointed at `AM_W_AK105_Fire` on `SK_AK105` while the mesh is on `SK_Pistol`, so it failed silently every shot — a project-wide sweep of all 276 montages found NONE on `SK_Pistol` except the reload, so `AM_W_Extractee_Pistol_Fire` was authored wrapping `Anim_Weapon_Pistol_Fire` (5 bone tracks — root/trigger/slide/magazine/hammer — all present on the re-rigged mesh; config byte-identical to the reload sibling except no `PlaySound` notifies, since fire audio comes off the DA's `FireSound` and a notify would double it); `ABP_ExtracteeMain` had all 8 `CoverAlign*Transform` serialized to IDENTITY where the primary simply runs the C++ defaults, so a ~90° yaw + 20 cm hand correction applied to one and not the other in every cover pose; `MagazineSize` 15→22, because `BurstCount` is the reload-GATE threshold not a burst length, so 15 rounds meant ducking to reload roughly every other peek. C++ side: commit `17bf1f5f` had added `IsPrimaryCompanion()` to the follow-slot EQS gate, silently disabling for the VIP the fight-aware follow that `66f10bc3` added to stop "the trailing extractee strolling through firefights" — restored via new `UEnvQueryContext_FollowLeader` + `ACompanionAIController::SetFollowLeader`/`GetFollowLeader`, with `EQS_FollowSlot`'s donut Center AND both tests (`Trace.Context`, `Distance.DistanceTo`) repointed off `EnvQueryContext_Player` (user call: score on the leader too, or slots generate around the primary but bias back toward the player). Cover anchor, back-bias zeroing and standoff clamp in `BTService_CoverSwitchMonitor` now match the follow anchor bit-for-bit; overwatch anchor leader-measured (`bPlayerAnchored`→`bFormationAnchored`). ⚠️ `WaveHoldLeashDistance` and `NormalCombatLeashDistance` deliberately STAY player-anchored — they ask "has the party left the player behind", and chaining them lets a secondary sit at twice the intended distance with neither tripping. **Also fixed a live friendly-fire bug in `WeaponBase`** (affects every shooter, not just the companion): `RebuildFFIgnoreList` cleared its cache BEFORE three early returns and stamped only after, so a transient null owner left the list EMPTY and the next hitscan fired with no team exclusions at all — `Reset()` moved below the guards so a failed rebuild preserves the last good list. Companion muzzle/LoS withhold traces now use the weapon's own FF ignore list, since friendlies were being treated as blockers for shots the bullet passes straight through. Mode identity CLOSED by user 29/07: "extractee on defensive is fine" — `SetMode` stays pinned to `Normal`, do not revisit.
- [~] [Stretch] **Weapon system.** Full arsenal — AR / pistol / SMG / shotgun; 2-weapon loadout (primary + pistol; some missions pistol-only); attachments with effects; per-weapon realistic recoil; a reusable skill + doc for the right-click ADS (aim-down-sights) alignment so adding new weapons is easy; player grip polish. Weapons/attachments from the Infima ModernGunsBundle; visuals stay on the procedural FPS pack. — In flight on branch FPS-pack-weapons: plan at `agent_docs/fps_pack_weapons_plan.md`. Rifle full loop DONE (moving-parts reload, 3P mesh, fire polish) and rifle attachments DONE + playtested via the kit modding screen (irons/holosight/scope with ADS through each, suppressor with flash muting, vertical/angled grips, laser with corrected beam; tracer origin + FP muzzle flash orientation fixed in C++, data-driven via DA). Blender re-rig DONE (SKM_InfimaAR_KitRig on the kit rifle skeleton — mag/bolt/trigger/dustcover ride kit bones, kit-native reload/fire anims, BP_Weapon_InfimaAR duplicate with the socket-attach hacks retired) and validated out-of-the-box in the pristine kit demo project; the long-standing grip fault was isolated to our forked player character (~4cm pose divergence with all data assets proven stock-identical). GRIP FIXED: the divergence was our modified anim stack — swapping ABP_Manny + ABP_FP_ArmsProcedural + AC_ProceduralAnimation in place with pristine kit copies restored demo-perfect arms on the existing character (no player rebuild needed); gun position user-tuned via the scripted Blender nudge loop; reloads forced to the empty (bolt-cycle) variant via the item's Arms_ReloadAnim. Player mesh DONE (SKM_Character_73 on kit SK_Mannequin). Handguard-as-attachment DONE + playtested: barrel+handguard stripped out of the SKM in Blender into a new Handguard slot component on BP_Weapon_InfimaAR, new ENUM_Handguards (Default/Short/Extended) + Handguard member on kit ST_Attachments, SpawnAttachments override swaps the guard mesh and shifts Muzzle/Laser/FrontSight along the barrel per variant (suppressor, laser, front sight, muzzle flash and tracer origin all ride the shifted comps; rear iron counter-shifted to stay on the receiver), and a fifth HANDGUARD dropdown wired into the kit's UI_WeaponModding menu (options from the enum, synced on open, writes item data and re-applies live; kit set-members nodes patched so other slot changes preserve the handguard choice). Infima Pistol re-rig DONE (SKM_InfimaPistol_KitRig on the kit pistol skeleton — slide/trigger/mag ride kit bones, mag joined in Blender, bind pose diffs 1e-6 vs kit mesh; BP_Weapon_InfimaPistol duplicate, bare irons with slot vars nulled + hidden FrontSight2/RearSight2 aim comps for kit ADS; DA_Pistol + DA_Pistol_Suppressed) and 2-weapon loadout C++ DONE + playtested (WeaponComponent primary/secondary slots with per-slot ammo persistence, 1/2 key switch via IA_EquipPrimary/IA_EquipSecondary in IMC_Default, pistol default secondary on BP_ExtractionCharacter). Pistol attachments DONE + playtested via the kit modding screen (palette meshes per slot, slide rail auto-shown/hidden with mounted gear, holosight/scope ADS precedence over the baked irons via an OpticSight-mesh sync gate, FireKick procedural fire feel bridged to the kit arms). Infima SMG DONE end-to-end + PIE-verified (SKM_InfimaSMG_KitRig re-rig on the kit rifle skeleton with baked sights/handguard face-cut out in Blender; BP_Weapon_InfimaSMG with BP-is-truth slot positioning — comp template positions captured as bases on first SpawnAttachments so in-editor placement is exactly what ships; SMG handguard swap Default/Extended with +12.26 rider shift, rear iron counter-shifted, grip pinned; FrontAimPoint/RearAimPoint sockets added to the SMG sight meshes for kit iron-sight ADS; DA_SMG + DA_SMG_Suppressed at 900rpm/22dmg with close-range falloff + lighter recoil; BP_SMG gameplay actor; SMG set as default primary for testing). Infima Sniper BUILT awaiting PIE (SKM_InfimaSniper_KitRig re-rig on the kit sniper skeleton — bolt/trigger/mag-release ride kit bones, mag + spare-mag + feed-bullet geometry joined and weighted to Magazine/2nd_Magazine/Bullet per kit convention, bind-pose roundtrip 0.0, kit FireLong bolt-cycle verified on the mesh; BP_Weapon_InfimaSniper duplicated from the kit sniper item BP so bolt-action fire/reload anims come free; FIXED Infima scope as its own component with hidden aim comps on the measured optical axis for ADS — SIGHT menu row forced inert by writing Sight=Empty into item data before the parent SpawnAttachments; NEW swappable BARREL slot (Default/Short/Extended, muzzle + flash/tracer ride the barrel-length delta) alongside the SMG-pattern handguard swap (laser/grip ride its delta); ENUM_Barrels + Barrel member on kit ST_Attachments + sixth BARREL dropdown in UI_WeaponModding reusing the handguard write-path, kit set-members nodes patched for the new field; DA_Sniper + DA_Sniper_Suppressed — semi 0.5/s bolt pacing, 90dmg, long falloff hold, 5-round mag, ADSFOV 30 scope zoom, kit 7Mag bolt-action gunshot audio; BP_Sniper actor; sniper set as default primary for testing). Sniper scope reticle + PiP quality pass (saved, PIE untested): replaced the parallax-offset physical reticle plane (Infima slot-2 mesh, hidden via a new unlit-translucent M_KitScopeSightHide override) with a procedural mil-dot duplex reticle + edge vignette drawn into M_KitScopeRender via Custom HLSL (dead-centre on the PiP image, tunable scalar params in group Reticle); PiP image lag fixed by locking ScopeCapture world rotation to the player camera every tick (was riding the sway-driven weapon mesh); RT_Sniper_Scope 1024→2048. Next: loadout-menu gate.
- [~] [Stretch] **Ammo economy.** Ammo drops from killed enemies (drop-chance pickup class) and is lootable from rooms; one ammo type per weapon type; ammo the companion loots is auto-added to the player. — C++ done (UAmmoDropTableDataAsset chance/amount per EnemyWeaponAnimType, AAmmoPickup overlap collect on category match, container ammo grants via subsystem). Full pickup-system round BUILT (awaiting PIE): typed ammo routing fixed (player DA_SMG/Pistol/Sniper ammo categories were all Rifle — flipped, +suppressed variants; ammo now feeds EITHER carried slot weapon, not just held), BP_AmmoPickup_Rifle/SMG/Pistol/Sniper children with mag meshes, DA_AmmoDropTable category rows repointed. WEAPON PICKUPS: kit BP_Item_Pickup loop bridged to C++ (WeaponComponent::ReplaceSlotWeapon slot swap w/ ammo restore + refusal gates mirrored in BP; 4 BP_Pickup_* weapon children with kit-rig meshes; WeaponPickupRef drop loop; OnWeaponEquipped slot-write + attachment passthrough fixed so modded guns keep attachments across switch/drop). ENEMY GUN DROPS: WeaponDropTable keyed by enemy weapon class (AK enemies 25% -> AR pickup, Rusher 25% -> SMG pickup, partial-mag "smart random"; gun drop replaces that kill's ammo drop; LMG/revolver/shotgun/sniper enemies never drop guns). ATTACHMENT PICKUPS: BP_Attachment_Pickup swap-in-place (displaced attachment spawns as pickup, empty slot despawns, inert meshless options skipped); DT_Item/SG_Character REINST-save healed. Compatibility gate rebuilt per-gun data-driven (six `Accepted*Options` byte-lists on UWeaponDataAsset, curated per weapon DA incl. _Suppressed; IsCompatibleWithHeldWeapon BP function gates interact + shows "Won't fit this weapon" prompt on focus; old dead Class-Is-Child-Of gates removed, Grip/Barrel dead-ends fixed); 8-pickup test row placed in DemoMap near PlayerStart. Pending: PIE pass (drops, swaps, typed ammo, attachment swap + compat refusal), rusher rifle-ammo/SMG-gun asymmetry decision. **Weapon cases + loot volumes LANDED (26/07):** `AWeaponCase` presents Infima-style open gun cases whose foam sockets hold individually-pickable pickup actors — per-case-type `SlotDefinitions` (socket + pickup class + preview mesh + offset) authored once, per-instance `FilledSlots` as a `GetOptions` dropdown, editor previews in `OnConstruction`, real pickups spawned attached on `BeginPlay`; `OnCaseItemPreFinish` fires before the pickup's construction script (the fix for attachments seating flat instead of in their cutout) and `OnCaseItemSpawned` shrinks the kit's 24 cm interact collider. `BP_WeaponCase_AssaultRifle` wired with all 9 Infima sockets + the hand-aligned stim slot; case mesh duplicated to `/Game/Core/Meshes/World/SM_WeaponCase_AssaultRifle` with complex-as-simple collision because Infima's box hull sealed the interior against the kit interact trace. `BP_Stim_Pickup` added (kit `BP_Interaction_Base`, `AddStims(1)`, refuses when the pouch is full). `bLieFlat` flag on `BP_Attachment_Pickup`. `ALootContainer` is now an invisible `UBoxComponent` volume with an editor billboard, implements `IWorldInteractable` (it previously had no HUD prompt at all), replicated `bLooted`, and blocks only a NEW `Interact` trace channel (`ECC_GameTraceChannel2`) so it can't blind enemy LOS / muzzle-clearance / CoverGen — player interact + companion ping traces run a second pass and take the nearer hit; 4 gym containers re-placed as volumes, `BP_LootContainer1` duplicate deleted. **Case item selection FIXED + PIE-verified (26/07):** "look at it ≠ focus it" had two stacked causes — (1) `bLieFlat=false` skipped the WHOLE `FlattenDisplayMesh`, including the re-centring, so seated meshes rendered at their raw pivot offset (handguards 16–23 cm, suppressor 8.8 cm) away from their actor-origin hitbox; now the flag gates only the rotation (`PickA = tallTest AND bLieFlat`) and re-centring always runs, with the case reading each item's re-centre shift back and `AddActorLocalOffset`ing the inverse so items still land in their foam cutouts (generic, no per-slot data); `ItemCollisionSize` 4→8. (2) Four stray `TEMPMEAS_*` measurement pickups left by an earlier in-engine pass sat loose next to the placed case, reading as misplaced duplicates. `pitfall`: in-engine agents that spawn temp actors must be re-checked for leftovers — the pass that spawned these reported them destroyed. Still pending: DemoMap container re-place (user). **Pistol/SMG/Sniper cases AUTHORED (27/07):** `BP_WeaponCase_Pistol/SMG/Sniper` + `SM_WeaponCase_*` ComplexAsSimple mesh dupes, per-gun fills gated on each DA's `Accepted*Options` (pistol: suppressor/scope/laser/ammo/stim, no grips; SMG: + grips + Extended handguard; sniper: suppressor/handguards/barrels/ammo/stim only — no laser/grips/sights), 3 new pickup children fixing the shared display-mesh map showing wrong-gun meshes (`BP_Attach_HandguardExtended_SMG`, `BP_Attach_HandguardShort_Sniper`, `BP_Attach_HandguardExtended_Sniper` via the base's `DisplayMesh` override); test instances placed in L_EnemyGym beside the AR case (X=1700, Y=-7630/-7380/-7130; AR at -7880), PIE pass pending, 4 Infima case types remain (LMG/Revolver/Shotgun/RocketLauncher). **Scale + gun-in-case LANDED 28/07 (awaiting PIE):** all four case BPs default to 1.5 (root scale on the CDO) and each carries a socketless `Weapon` SlotDefinitions row so the gun itself is lootable out of the foam — `BP_Pickup_Rifle/Pistol/SMG/Sniper` with `SKM_Infima{AR,Pistol,SMG,Sniper}_KitRig` previews, Offsets lifted straight from Infima's own `BP_GunCase_*_Example` gun placements (their "Gun Case" mesh component sits at identity, so those child-actor transforms are directly reusable; all four are pitch-90, translations AR (2.65,-10.45,10.89) / pistol (-3.57,-1.07,12.48) / SMG (-10.49,-14.72,13.00) / sniper (1.05,-33.97,12.38)). Offsets are Infima-derived, NOT visually tuned against our re-rigged kit pickup meshes — expect a nudge. The kit `BP_Item_Pickup` BeginPlay ground-settle (`LineTraceByChannel` → `SetActorLocation`) needs no gate: `SpawnSlotItems` already re-runs `SeatItemInSlot` after `FinishSpawning`, which re-applies the seat over anything BeginPlay moved. Two tooling gotchas worth keeping: NeoStack reaches an inherited native root only through the CDO subobject path returned by `invoke(class, "K2_GetRootComponent")` (dot-path `set` and `get_object` both fail), and because `SlotDefinitions` is `EditDefaultsOnly` a placed instance keeps its construction-time snapshot — editing a case BP's slot list means **re-placing** every instance, since neither a recompile nor an instance-side `set` will update it. ⚠️ **That re-placing warning is now STALE (29/07):** `SlotDefinitions` was replaced by per-cutout `UWeaponCaseSlotComponent` SCS components, which propagate to placed instances normally — adding a cutout no longer requires re-placing anything, it just appears in each instance's `FilledSlots` dropdown. **Missing attachments ADDED to AR/SMG/Pistol cases (29/07):** each gained `Slot_SuppressorShort` (`BP_Attach_Suppressor`, Muzzle byte 1), `Slot_Torch` (`BP_Attach_Torch`, Laser byte 2) and `Slot_Holosight` (`BP_Attach_Holosight`, Sight byte 1), seated by copying the case's own `Slot_Suppressor`/`Slot_Laser`/`Slot_Scope` transforms so each reuses an existing foam cutout — designer picks one per crate via `FilledSlots`, overlapping seats are harmless because an unticked slot spawns nothing. Zero DataAsset edits needed: DA_AssaultRifle/SMG/Pistol already carried `AcceptedMuzzleOptions=(1,3)` and `AcceptedLaserOptions=(1,2)`/`AcceptedSightOptions=(1,2)`. All 34 slots now parent to `CaseMesh`; ⚠️ VibeUE `add_component(parent_name=…)` silently drops onto the root when the named parent is a native C++ component — `SubobjectDataSubsystem.attach_subobject` is the working fix and preserves relative transforms. Note `Slot_Suppressor` spawns the LONG suppressor (byte 3) — deliberately not renamed, since placed instances reference the id in `FilledSlots`. **Details-panel QoL (29/07, built + user-verified):** `AWeaponCase` got `PrioritizeCategories="Case"` and `FExtractionEditorModule` now registers a "Case" property section, so the category has its own filter pill and floats to the top; `PrioritizeCategories` confirmed present on the generated BP child classes, not just the native class. Sniper case deliberately untouched. **Case item seating FIXED + PIE-verified (29/07):** items spawned 90° pitched from their previews because `SeatItemInSlot` snapped the pickup's *actor root* to the slot while the visible mesh sits at an authored offset under that root (`BP_Pickup_*.Weapon` and `BP_AmmoPickup_*.Mesh` are both P90). It now seats the *display mesh* on the slot, reading the offset from each component's **archetype (SCS template), never its live transform** — critical, because `BP_Attachment_Pickup::FlattenDisplayMesh` recentres attachment meshes on their local bounds from the construction script, and inverting that live value threw every attachment out of its cutout. `FindItemDisplayMesh` must exclude `UWidgetComponent` (it inherits `UMeshComponent`, and the kit's interaction widget sits on every pickup). Authoring side: `Preview_Weapon`'s offset folded into `Slot_Weapon` (AR/Pistol/Sniper) and the mag's authored P90 folded into `Slot_Ammo` (all four cases) — the ammo P90 is its lie-flat orientation, NOT a stray offset to cancel, so cancelling it stood the mag on its end. Preview and game now agree for every slot type. ⚠️ **Loose end:** the new `SeatVerify` check logs a MISMATCH warning per attachment on every play (they legitimately sit up to 15 cm off their slot after the kit's recentring) — scope it to pickups without runtime repositioning. — **Weapon-drop round (29/07):** reserve ammo on enemy gun drops is now whole magazines only (`FWeaponDropEntry` MinReserveMags/MaxReserveMags 1-4 + optional `RoundsPerMag` override for the 32-round enemy SMG that hands over a 30-round player SMG); in-mag rounds roll 10-30 clamped to mag size. Dropped guns now fall under physics and settle instead of hanging at hand height: invisible proxy box on `BP_Item_Pickup` sized from the mesh bounds, zero-restitution `PM_DroppedWeapon`, Pawn/Visibility/Camera ignored so it can't be kicked or block shots, baked to a static transform once the body sleeps. Attachments now survive drop+repickup: the drop passes all six slots (Grip/Handguard/Barrel were never carried), and the collect path writes the slot BEFORE `ReplaceSlotWeapon` so the kit's equip guard sees the incoming weapon and stops wiping `ResolvedAttachments` on a class mismatch. ⚠️ **Open:** dropped guns still render bare — the kit's visual weapon actor self-attaches to the player on BeginPlay (`SocketOffset`), so it can't be spawned as a world prop without gating `BP_Item_Base`'s held-weapon setup. Handed to another chat. **Walk-over ammo + crate markers (30/07):** ammo drops now collect on contact instead of needing an interact press -- `AAmmoPickup`'s sphere overlaps `ECC_Pawn`, any friendly pawn (player, companion, rescued extractee -- resolved by team attitude, so enemies and corpses never collect) trips it, and the grant is always routed to the PLAYER since the companion has no inventory. Refusal is silent on the walk-over path (compatibility pre-checked for every collector, so strafing a drop can't re-toast "no compatible weapon"); the toast stays on the deliberate interact. One attempt per pawn per overlap, cleared on end-overlap. Case-seated ammo boxes are exempt via the existing `GetAttachParentActor()` return, so brushing past a full crate can't vacuum it. ⚠️ Enabling pawn overlap exposed a latent bug: `SetCapsuleHalfHeightWithFloorAdjust` used `OverlapAnyTestByChannel`, which counts overlap-only volumes as obstructions -- an uncollected drop under the player silently refused prone->stand. Now blocking-only, which also unblocks the pre-existing hazard from doorway triggers / objective steps / stealth volumes. Weapon crates gained the same overhead ring `ALootContainer` carries (`ULootMarkerComponent` on `AWeaponCase`, `WBP_LootMarker` + per-case Z offset clearing each lid: AR 92 / SMG 101 / Sniper 105 / Pistol 64), lit while the crate holds items and cleared on the last collect. The marker component gained an optional owner-bound availability query evaluated on its existing poll, for owners whose looted state fires no event; `IsEmpty()` also counts a hidden AND collision-disabled item as taken. Recon confirmed every current case pickup destroys itself on collect, so that fallback is currently dead code.
- [x] [Stretch] **Player grenade + companion grenade lobs.** Player: CoD/BF-style quick grenade on key 3 (stim moved to 4) — one press swaps to the kit throwable with no equip anim, auto pin-pulls and throws, then auto-returns to the held gun; 1-in-hand + 2 reserve; kit BP_GranadeProjectile retuned to the enemy-grenade damage model (150 dmg / 4 m radius, linear falloff, team-blind = friendly fire on player + companion), velocity-based lob arc (crosshair-at-body-height lobs up and over), 1.8x visible mesh, reduced tumble + post-bounce angular damping, louder pin-pull, inherited gun-attachment slot meshes cleared on throwables. Plumbing: kit throwable slot enabled (was 0-ammo, and the fork's SwapWeapon event was a dead stub — quick-throw enters the swap flow directly), C++ throwable-fire diversion (WeaponComponent flag routes fire to the kit item's BeginFire, guards reload/ADS, restores kit gun visual on 1/2 switch, no shoot-takedown broadcast on grenade press). Companion: full Grenadier-system reuse (UEnemyGrenadierComponent + BP_EnemyGrenade + AM_Enemy_GrenadeThrow montages on the same skeleton/UpperBody slot, release anim-notify generalised to non-enemy throwers) triggered from the combat task when its target stays LOS-blocked ~2 s — throws at last-known, refuses when the player stands inside blast radius + buffer, cancels on DBNO/task exit; tuning in DA_CompanionTuning (supply 4, 18 s cooldown, 120 dmg / 3.5 m). Player quick-throw PIE-iterated + user-approved; companion lob PIE pending. **Throw bug-fix + feel round (07-26, user-approved):** grenade no longer tunnels through walls or vanishes on a moving throw — CCD enabled on the projectile body (it was clearing its own 17 cm capsule width in one 18 cm discrete step), spawn moved off the `hand_r` bone (which sits inside the player capsule, with Owner/Instigator unset and nothing ignored — it spawned interpenetrating and got depenetration-launched) to a camera-origin point with a sphere-trace clearance clamp, Owner/Instigator wired, mutual `IgnoreActorWhenMoving` between player and grenade; mass 100 kg → 0.45 kg. Release now driven by a real `AN_GrenadeRelease` notify placed on the actual hand-extension frame (both clips had zero notifies; the spawn was a hardcoded 0.7 s delay) with a DoOnce fallback, and press→release cut 1.933 s → 0.648 s (pinpull rate 2.8x, throw 1.9x, blend-out 1.0→0.25, all `PlayLength − X` delays literalised since that return is rate-independent; in-hand mesh swap/hide re-timed to match). Aim fixed — the fixed 560 cm/s `ActorUpVector` term (constant world-up regardless of pitch, so the throw only matched the crosshair at one aim angle) replaced with camera-forward × 1150 + world-up × 380 + 0.5x inherited player velocity: ~12.4 m level throw, 1.08 s flight, 16.4 m sprinting (was 27.5 m). Visible mesh 1.8 → 2.34. Known ceiling: past ~27° of upward aim the 2 s fuse airbursts before landing. The `IA_ThrowableSlot` entry path still adds its own 0.35 s equip delay (~1.0 s end-to-end on that key) — left as swap-anim cover. **AI lob arc-clearance fix (02/08):** `UEnemyGrenadierComponent` (shared by the companion and the enemy Grenadier) only probed 150 cm straight off the muzzle, so a solved arc could still smack a ceiling/doorway header and drop the grenade back on the thrower. Commit now simulates the whole parabola against WorldStatic via `PredictProjectilePath` and requires the first blocking hit to be within 200 cm of the intended landing point; four arc heights are tried in order (0.5 medium, 0.68/0.85 flatter for low ceilings, 0.35 higher for tall cover) and the first clear one wins. The chosen arc is stored and reused for the release-time re-solve — that re-solve was hardcoded to 0.5 and would silently undo an indoor-safe pick. Candidates exceeding the projectile CDO's `MaxSpeed` are rejected (velocity is clamped every tick, so those landed short). No arc clear = no throw, retried on the next window. **Bark/throw desync fixed:** the companion's "frag out" fired at telegraph start, so a wind-up cancelled by target death / a new command / branch abort left the call with no grenade — new `OnGrenadeThrown` broadcast fires only after the projectile spawns and the bark hangs off that. Enemy grenadier's telegraph bark deliberately unchanged (there the early warning is the point).
- [ ] [Stretch] **Enemy gun attachments.** Add a few attachments to enemy weapons.
- [~] [Stretch] **Player-commanded companion cover + "cover me".** Two new orders giving the player direct control in a firefight. *Cover There:* ping any wall/crate with the existing ping key, confirm, and the companion walks there and takes the best position on that specific piece of cover — end of a wall with a firing angle rather than the middle — holding the cover pose out of combat and fighting from it for 8s once a fight starts before normal cover switching resumes. No distance limit — the ping trace range is the limit — though cover is matched to the pinged wall's own floor; where the companion takes cover is otherwise the player's call. *Cover Me:* fourth row on the X mode menu (key 4), live only with a combat target, giving a ~5s window of sustained peek-fire that ignores low-health caution, takes reduced damage, fires at a target's last-seen position when it breaks line of sight, and pauses its clock to reload before resuming. 10s cooldown, countdown on the menu row. *(New: `BTTask_CompanionTakeCover`, `BTTask_CompanionHoldCover`, `BTTask_CompanionClearCommand`, `CompanionCover::FindCoverOnPingedWall`. Reuses the AICS query, shared scorer, reservation subsystem and `BTTask_MoveToCoverPoint` rather than new cover machinery. Companion stall detection added — it previously had none, being gated on enemy archetype data.)* Standing/tall-wall cover fixed + playtested: the ping search radius is now lateral only (a high impact on a tall wall no longer spent its whole budget on Z) with a one-directional Z gate so a same-facing wall on another floor can't be picked, and stand-height candidates test peek-shootability from the reach-bounded corner apex instead of a fixed 65cm lateral offset that traced inside the wall. Confirming Breach/Loot/Explore/Takedown now clears the commanded cover hold and forces a clean cover exit on both the abort and tick paths, so breaching out of commanded cover no longer plays in the cover pose. **Cover Me still in PIE iteration.**

### Week 3/08 (W7) — UI/UX, polish, integrate (full-time in NY) — target everything done Sun 9/8
- [~] [Core] **In-game HUD: HP + ammo** (required). Player health and a weapon/ammo readout. **Easy HUD Builder restyle (04/08):** the HUD is driven by the EHB plugin — `DA_ExtractionHudConfig` → `DA_ExtractionStyling` (colours/fonts) + `DA_ExtractionModules` (17 modules, per-module anchor/offset/size). ⚠️ Module layout MUST be edited on `Contexts → Gameplay`, not `Default Module Layout`: `DefaultHudContext` is `Gameplay` and its `Override Widget Default Values?` is True, so the default is dead. The visual placer is `EUW_EasyHudBuilderAssistant` (drag/resize canvas + anchor grid); `EUW_EasyHudModulesListing` is rename-only and is NOT it. Palette moved off flat white to dark plates + a grey ramp, accent orange reserved for objectives. ⚠️ **`DA_ExtractionStyling` only drives the plates** — bar fills, icon tints and text are per-widget properties that ignore it, and `WBP_EHB_SkillConsumableDisplayer::SetSlotEnabledState` re-pushes `(1,1,1,1)` onto its icon every refresh (pin edited, not the widget default). ⚠️ Colour values are LINEAR: `0.76` linear renders ~`0.90` on screen, so "mid grey" is ~`0.42`. Health/shield collapsed to one row (health keeps the bar, shield is icon+number; `ShieldBar` survives hidden at zero fill weight because the graph still drives its percent). Weapon panel rebuilt to BF layout — name over icon + `13 /126` on one plate. All 17 `WeaponDataAsset`s had **empty `DisplayName` and empty `HudIcon`** (the AK silhouette was `BP_ExtractionHudBridge`'s fallback texture); names filled, icons still unauthored. Objective bullet aligned via a fixed 25px SizeBox matching the size-20 font's line box. `WBP_Mod_CompanionCommand` gained a plate + `COMPANION MODE` label. Pending: second-weapon row (bridge `OnStowedWeaponChangedBP` added, widget row not built), user-authored icon art, chamfered plate shapes (EHB draws `RoundedBox` only — needs a brush/material). **Stim slot fixed + fleshed out (26/07):** the root cause of "key 4 does nothing" was that `IA_UseStim` was never mapped in ANY live context — the action asset, the character CDO reference and the C++ bind all existed, but no key was bound (now `IMC_Default` → `Four`; the kit's `IMC_Player` owns T-takedown / F-interact / 3-grenade, `IMC_Default` owns fire / reload / 1-2 switch / companion keys, and `Four` was free in both). Two silent refusals compounded it: the player spawned with 0 stims and a stim is refused at full health, neither logging or toasting. Now: `StartingStims` (default 1), both refusals routed through the loot-toast channel, stim spent on commit with the heal landing mid-animation (`StimHealDelaySeconds`) instead of instantly, and a committed-use window that locks fire / ADS / reload / weapon-switch for the duration (cancelled on DBNO, death and EndPlay). Verified working in PIE (heal on 4). Animation + prop + HUD landed: `OnStimUsed`/`OnStimUseEnded` drive the `AM_Injection` upper-body montage (`UpperBody` slot on `ABP_Manny`; the QuantumCharacter-skeleton clip plays because `SK_Mannequin` lists it in `CompatibleSkeletons`, same route `AM_BeingRevived_Player` already uses) with `ShowStimProp`/`HideStimProp` notifies, force-hide on Completed/BlendOut/Interrupted so the prop can't strand. `UI_Hud`'s `StimKeyText` 3→4, and `StimCountText.TextDelegate` was `(null).None` — `GetStimDisplayText` existed but was never bound, so the count was a static design-time `x0` that could never move; now pulled on Construct via `BroadcastStimCountNow()` and refreshed on Tick. **Pen placement (`pitfall`-grade):** the FPHealing pack attaches `SKM_InjectionPen` to socket `weapon_rSocket` (bone `weapon_r`) at (0,5.5,0)/(0,180,90)/0.5 — but its animations ALSO drive the `weapon_r` bone to a constant (-7.648,3.037,-0.741)/(10.495,-8.344,93.397), and our retargeted `AS_Injection_Type02` is on the QuantumCharacter skeleton which has no `weapon_r` track at all, so that bone sits at ref pose here. Copying the pack's socket+transform lands the pen ~6.8 cm and ~90° off. Fixed by baking the pack's animated `weapon_r` offset into the component and attaching to `hand_r` via `ItemHand_R`: loc (-6.706116, 2.569705, -6.139830), rot (-10.495432, 171.656094, -3.397171), scale 0.5, all-`KeepRelative` attach rules + explicit set-relative-transform after attach. Pen scale normalised to a 11.8 cm pen (that one clip's `weapon_r` track carries scale 0.5 where every sibling clip has 1.0); literal-pack alternative is scale 0.25 at loc (-7.177227, 2.803502, -3.440590). Placement is numerically derived, NOT visually verified — the editor won't evaluate animation on a script-spawned SkeletalMeshActor, so a scrub screenshot needs PIE. **Injection anim FIXED + PIE-verified (26/07):** the clip never really played because `AM_Injection` sat in the `UpperBody` slot, which feeds the layered blend *upstream* of the `ABP_FP_ArmsProcedural` linked graph — the kit's sway/lean/recoil and its two-bone hand IK all run after that slot and re-solved both hands back onto the gun every frame, so the authored arms were overwritten and the pen (parented to `ItemHand_R` → `hand_r`) rode the IK'd grip hand instead of the pose the clip wanted. Fix is a new `ArmsOverride` slot plus a `spine_01` / BlendDepth-1 branch-filter `LayeredBlendPerBone`, inserted *downstream* of the linked graph and upstream of the existing `FullBody` slot, with the arms-procedural pose routed through a `Save Cached Pose 'ArmsProceduralPose'` + two `Use Cached Pose` nodes so the subtree isn't double-updated (it drives both the blend's base pose and the slot source — a raw fan-out would double-advance the kit's spring-state sway/recoil). `spine_01` mask = montage owns torso/head/both arms/hands, legs stay on locomotion so movement still reads. `FullBody` deliberately left alone — vault, mantle, takedown and the reviver kneel need the true full-body override. `ArmsOverride` registered in `SK_Mannequin`'s DefaultGroup. C++: `SetHeldWeaponHidden(true)` in `HandleStimUsed` after the ADS/pose drop (kit `NewHandPose` re-shows hand-socket items if you hide first), guarded re-show in `HandleStimUseEnded` on `!bIsDBNO && !bBeingRevivedAnimActive && !bIsReviving`; `InteractStart` and `TryStartTraversal` now refuse on `IsUsingStim()` (looting ran `ReplaceSlotWeapon` → `SetActiveWeapon` and un-hid the gun mid-window; a vault killed the montage while the window and its hide kept running). BP `Stop Anim Montage` now names `AM_Injection` instead of stopping whatever montage is current. **Camera + leg-blend follow-ups CLOSED as won't-do (26/07) — do not re-attempt without new information.** Free head-look during the injection was built and reverted (`43310b5b`, reverted by `d9b83918`): the camera is DELIBERATELY animation-driven here. `SpringArm` is socketed to `head` with `bUsePawnControlRotation=False` and inherit pitch+yaw `True`, so the view direction IS the animated head bone — looking around normally works by control rotation driving the AnimBP to turn the body/head, which is why any full-body-ish override severs it. Note for anyone touching this: `bUseControllerRotationYaw` stays true through the window, so YAW input already turns the capsule and is visible during the injection; only PITCH is swallowed by the montage. A control-rotation bank/restore was also built and dropped by user choice — restoring yaw whips the view up to 180° in one frame and kicks the kit's turn-in-place chain (`TurnDir`/`UpperYaw` = clamped delta ×10), and the pitch-only variant wasn't wanted either. **Leg blending during the injection is a known accepted limitation:** with the montage in `ArmsOverride` (downstream of the `ABP_FP_ArmsProcedural` linked graph) the arms play correctly but the legs hold the clip's standing pose while the capsule moves; with it in `UpperBody` (upstream) the legs blend fine but the procedural arms layer + its two-bone hand IK overwrite the arms. Tried and rejected: `spine_01` branch filter, the kit's `SK_Mannequin:UpperBody` blend mask, `bMeshSpaceRotationBlend` on/off, and gating the linked arms graph off via an `IsUsingStim()` bool bypass — none changed the legs, and the leader/follower split (`Soldier` IS a correct `SetLeaderPoseComponent` follower with `FollowerShouldTickPose=false`), root motion and duplicate slot nodes were all eliminated. Root cause never isolated statically; it needs a PIE bone-sample of `thigh_r`/`calf_r`/`foot_r` with and without the montage, which the user declined. Current shipped state is the downstream `ArmsOverride` version — arms correct, legs static, camera animation-driven, read as a committed animation. Pending: a PIE nudge on the pen transform if it reads off. **Bespoke HUD + corrected v2 art (04/08, awaiting PIE):** the live HUD is no longer EHB — `BP_ExtractionHUD` BeginPlay creates `WBP_HUDRoot` and calls `HideOrShowEntireHud(true)` on the EHB manager, so the EHB tree is hidden but `BP_ExtractionHudBridge` still fans every channel to BOTH paths (wasted work, not a double-draw). All 23 corrected v2 panels/bars/details imported under `/Game/UI/HUD/ExtractionHUD/Textures`; **`T_Panel_Angular`/`AngularStrong` now at ZERO referencers** — every module has its own panel at its own draw size, `DrawAs=Image` (never nine-sliced; corner cuts are baked in). ⚠️ **`import_texture`'s `compression`/`lod_group`/`mip_gen_settings` opts are SILENTLY IGNORED** — all 46 landed as `TC_Default`/`TEXTUREGROUP_World`/mipmapped and had to be forced afterwards via `set()`; DXT + mips on a 400x6 accent or a 1px hairline border renders as mush. ⚠️ `ImportedSize` stays stale after a re-import over an existing asset; `Source.SizeX/Y` is the truth. Fixed this round: `HudIcon` authored on all 10 player weapon DAs (was empty on every one — the bridge pushed null and `Is Valid` left the design-time rifle up, which is why a pistol showed a rifle); equipment/weapon panels pinned to 259x101 / 422x101 so the rail stops resizing with the ammo digit count; amber accent corrected to 200x3 inset; keycaps resolve live through `UKeybindHintLibrary` (throwable **3** `IA_ThrowableSlot`, stim **4** `IA_UseStim`, primary **1** `IA_EquipPrimary` — note kit `IA_PrimarySlot` has NO keyboard binding, gamepad only, so it renders "D-Pad Left"); objective card wraps+clips and swaps panel art per row count (1/2/3-row textures at 280x124 / 408x240 / 408x296); health+armor bars rebuilt from `ProgressBar` to track-image + left-aligned clipped fill so the fill CROPS instead of stretching (spec requirement; armor 50/100 measures exactly half); companion idle/covering panel swap (258x54 ↔ 500x56) with the cover timer re-thresholded to **5** segments at 1.4/2.8/4.2/5.6 because the reference art's gaps sit on a 5×(42+3) grid, not 7. **Objective distance channel added:** `FHudObjectiveEntry.DistanceMeters` + id-keyed `OnObjectiveDistanceChangedBP` on a 5Hz timer with a 0.05m epsilon; measured from `GetPlayerViewPoint` (NOT the pawn) so the card agrees with the 3D marker; `-1` = no distance, collapses the text. Row wrap width dropped 304→248 / 176→120 to pay for the 56px distance column. ⚠️ **Unity-build gotcha bit again:** new anon-namespace constants in `Private/UI/*.cpp` MUST be prefixed — `UnrealUnitsPerMetre` and `DistanceBroadcastEpsilon` already exist in `ScreenMarkerProjection.cpp` and `ObjectiveMarkerWidget.cpp`, same class of collision as the `Ping*` rename. **`DefaultInput.ini` regression fixed:** commit `748e547f` (a HUD restyle) also wrote a full `[/Script/EnhancedInput.EnhancedInputDeveloperSettings]` block — `bEnableUserSettings`, `bEnableInputModeFiltering`, `EnableDefaultMappingContexts` — which is the prime suspect for stim-on-4 dying after it was PIE-verified on 26/07; block removed, needs PIE to confirm. **Compass fixed:** the yaw math in `WBP_Compass.RefreshCompass` (K=-8.5 px/deg, C=-2635) was always correct and `BP_ExtractionHUD` always called it — but `Get Control Rotation.Return Value` was never wired into `Break Rotator.Rotation`, so it pushed a constant `SetCompassYaw(0)` every tick. One wire. **`BP_ExtractionCharacter` Accessed None fixed:** `BeginSetup` is a MACRO (inlines at the call site) whose `Cast To PlayerController` had an unwired `Cast Failed` pin AND an unwired `Is Valid.Is Not Valid` pin — either dead-ended the whole `BeginPlay` chain, so `CreateHUD`/`UIcontrols`, hands slot, throwable slot, walk speed and pitch clamps were ALL lost whenever possession landed after BeginPlay. Objective copy shortened on OS_01/OS_03/OS_06 + PE_R1_OBJ_RoofBreach/StairBreach with `PromptAction`/`SecondaryPromptAction` cleared (they exist ONLY to substitute `{PromptKey}`; leaving them set with no token makes `AuditPromptKeyHint` warn every activation). Pending PIE: stim on 4, compass actually turning, distance readout, and the narrow 1-row card where a single unbreakable word >120px (`EXTRACTION` ≈125) overhangs into the empty distance gutter. **Playtest fix pass (04/08):** four defects off the first HUD playtest. (1) Attachment stat preview was invisible, not missing — it lived inside the EHB tree that `BP_ExtractionHUD` collapses via `HideOrShowEntireHud(true)`; moved into `WBP_HUDRoot` and the duplicate EHB module entry dropped, because the hidden copy was winning `RegisterAttachmentStatPreview` (last construct wins). (2) `WBP_EquipmentRail` ammo: `AmmoBlock` was `VAlign_Top` among `VAlign_Center` siblings and a fixed 250px `WeaponIconBox` left it ~65px of the 378px row — centred, icon box 200px, divider padding 40→24. ⚠️ STILL NOT RIGHT per user, second pass pending. (3) On-screen objective markers swam during camera turns: `UObjectiveMarkerWidget` interpolated position at `PositionInterpSpeed=14` even on-screen. Now snaps on-screen (matching `UPingMarkerWidget`); the old `bSnap` split into `bReset` (appear / boundary cross / bearing reversal, still drives the scale+fade interp) and `bSnapPosition`. ⚠️ **Screen-space markers reintroduced the camera-bob the world-space `AObjectiveMarkerDisplay` billboard was originally chosen to avoid** — reverting to it is the fallback if it still swims. (4) Keycap/READY text sat 3–5px high: `WBP_CompanionCommandMenu` + `WBP_CompanionStatus` drew each box Image and its label as two independent `CanvasPanelSlot` siblings, and **a `CanvasPanelSlot` has no `VerticalAlignment`**, so `STextBlock` renders top-flush — all 7 pairs rebuilt as Overlays (Image Fill / Text Center-Center), the pattern `WBP_EquipmentRail` already used. **Cover-me cooldown channel added:** the badge art existed but only ever received `OnCoveringFireTickBP`, the ACTIVE window — so it flipped to READY the instant the window ended while the 10s lockout ran. New `OnCoverMeStateChangedBP(bAvailable, CooldownRemaining, CooldownDuration)` on a 10Hz poll with edge dedup + `RefreshAll` replay, force-pushed from `HandleCoveringFireTick` so the handover has no gap; `WBP_HUDRoot.SetCoverReady` drives the badge and `SetCovering` now drives only the `WBP_CompanionStatus` strip. `bAvailable` is passed but deliberately NOT branched on — the badge has two authored states, so it reads READY when the lockout expired but cover-me is gated for another reason (needs a third state, director's call). Also fixed under review: a re-entrancy hazard where a BP handler of `OnObjectivesRebuiltBP` could refill the member `ObjectiveScratch` the outer frame was still reading (`bPushingObjectives` latch), and the distance epsilon raised 0.05m→0.5m against a whole-metre readout. **HUD V5 context-feedback pass (05/08):** director supplied an authored package at `Codex/2026-08-04/im/outputs/extraction-shooter-hud-v5-context-feedback` — 6 MasterReferences, 7 GameplayMockups, TYPOGRAPHY_AND_BEHAVIOUR.md + CONTEXT_FEEDBACK_LAYOUT.json, and text-free 1x/2x PNGs. It is the visual source of truth; all labels/keys/values render as live widget text. **Colour law: cyan = interaction, amber = objectives + ping markers ONLY, red/green = weapon worse/better and always paired with the arrow shape.** 31 2x textures imported to `/Game/UI/HUD/ExtractionHUD/V5/`; ⚠️ `import_texture` silently ignores compression/lod_group/mip_gen_settings AGAIN — all forced afterwards to TC_EditorIcon/TEXTUREGROUP_UI/TMGS_NoMipmaps/SRGB/TA_Clamp. Source PNGs are 2x design size; brush ImageSize = DESIGN size; panels DrawAs=Image never nine-sliced (baked chamfers). **All 7 modules built:** `WBP_ContextAction` (260x62, LIVE + PIE-verified — this is what finally renders the interact/rescue prompt), `WBP_ReviveProgress` (360x78, LIVE + PIE-verified, exclusive with ContextAction), `WBP_PickupFocus` (420x88), `WBP_AttachmentStatPreview`/`Row` restyled in place to the 440x176 design (C++ parent + BindWidget members preserved), `WBP_AreaNotification` + `WBP_AreaNotificationStack` (360x74, max-2 newest-on-top, 3.5s/5s), `WBP_TakedownChoice` (400x74 dual-choice, NOT a sentence), `WBP_CompanionWorldHealth` (180x34), plus `WBP_PingMarker` aim-vs-placed states. **Only ContextAction and ReviveProgress are wired** — the other five are built standalone and await a HUDRoot + bridge integration pass. ⚠️ **`WBP_RevivePrompt` retired** by clearing `RevivePromptWidgetClass` to None on BP_ExtractionPlayerController (asset kept on disk); it was the second on-screen revive prompt, hardcoded at `RevivePromptWidget.h:47`. Its `InteractHintFormat`/`InteractHoldingHint` were already dead in C++. **C++ follow-ups the build surfaced, none done yet:** stat rows need `BindWidgetOptional` ArrowWorse/ArrowBetter/VerdictText/ItemNameText/HeadingText + population (built, styled, Collapsed, waiting); `UKeybindHintLibrary` renders mouse buttons as "Middle Mouse Button" with no short-name table so MMB keycaps can't resolve live; the takedown prompt (`UPingPromptWidget`, hardcoded FText at `PingPromptWidget.h:81/:84`) subscribes to `UCompanionCommandComponent::OnPingChanged` directly and **bypasses the bridge entirely** — needs a new channel; no pre-ping "can ping" probe exists to drive the aiming state; `UEnemyDirectorSubsystem::OnDirectorWaveStarted` doesn't reach the bridge. **Area notifications need no new channel** — `AStealthDisciplineVolume` already emits Warning ("Discipline slipping") and Alert via `OnToastNotify` → `OnToastBP(Message, Severity)`, so it is a severity branch, not new plumbing. **Objective chain split (05/08):** `bOptional` added to `AObjectiveStep` (optional steps now register `NotTracked` instead of every active step rendering TRACKED) and ticked on OS_Opt_SearchRoom + OS_Opt_LootCabinet; new `EObjectiveCondition::CompanionModeSet` + `TargetCompanionMode` lets OS_07 complete on the actual mode switch, with new `OS_07b_ProceedThroughDoor` (DoorOpened on BP_Double_Door7) inserted after it — playtesters didn't know what to do after switching to stealth. OS_07 loses its marker (no sensible world target); the `ActivateActor -> OS_Opt_SearchRoom` effect moved to OS_07b to preserve the authored timing. `CompanionModeSet` added to `DerivesWorldState` + `ApplyCompletedWorldState` so a checkpoint resume past the stealth beat doesn't hand back a Normal companion. ⚠️ Colour values stored as raw /255 floats render pale — UE colours are LINEAR; `WBP_ReviveProgress`'s green needs a sweep. **V5 wiring closed out (05/08):** the white `[I] Breach` line was `UPingPromptWidget` — six hardcoded FTexts (`PingPromptWidget.h:78-93`) subscribing straight to `UCompanionCommandComponent::OnPingChanged`, **bypassing the bridge entirely**; its `PingPrompt` child is now out of `WBP_CompanionHUD` (asset kept). New bridge channels: `OnCompanionPromptChangedBP(ECompanionCommand)` and `OnPingCandidateChangedBP(bool, FVector)`. ⚠️ **`WBP_HUDRoot.RefreshPromptPanels` owns an explicit precedence — Revive > Companion command > Interact** — because ContextAction has two drivers that can be live at once and last-writer-wins flickers. Area notifications route off `OnToastBP` severity (Warning/Alert only; Info carries loot/door toasts). ⚠️ **Ping aiming needed C++**: `UPingMarkerWidget` collapsed its own root whenever no ping was placed and Slate doesn't tick a collapsed widget, so BP could never show the aim state — the marker now tracks a live CANDIDATE via `HasPingCandidate()`/`GetPingCandidateLocation()` and raises `OnPingAimingStateChanged(bAiming)`. That C++ event is the SINGLE driver; the bridge→HUDRoot→CompanionHUD `SetPingCandidate` route is retired (functions kept, call disconnected) and the old `OnPingMarkerAppeared → SetPingState(false)` hook removed — it fired AFTER the aim-state dedup and collapsed the ring mid-aim when sliding between targets. `UKeybindHintLibrary` gained a keycap short-name table (LMB/RMB/MMB/MB4/MB5/wheel) keyed on FKey identity, not the localised string. `IssuePing`'s trace factored into `ResolvePingHit` + a const `IsActorPingable` predicate so the aim ring only lights on a ping that would be ACCEPTED — reviewer verified `IssuePing` byte-for-byte unchanged. ⚠️ **`HealthWidgetComponent`'s OWN `WidgetClass` must be set, not just `HealthWidgetClass`**: `UWidgetComponent::BeginPlay` calls `InitWidget()` from the component property BEFORE `ACompanionCharacter::BeginPlay` reaches `SetWidgetClass`, so BP_Companion was casting the wrong instance — cast failed, `Initialize` never ran, and the takedown delegates chained off its exec pin never bound either. Attachment stat rows use a SYMMETRIC diamond, not the design's up/down triangles: C++ picks the glyph from `bIsImprovement` which is direction-agnostic, and 4 of 10 attachment rows are HigherIsBetter. 23 colour literals converted sRGB→linear across the V5 set. **STILL OPEN — `WBP_PickupFocus` (420x88 item nameplate) is built but unwired, and needs a DESIGN CALL, not code:** attachment pickups have no `ULootMarkerComponent` (only ALootContainer/ALootPickup/AWeaponCase do, and its contract is one FText + a 4-value enum — too thin). They host the kit's `UI_Interaction` on `BP_Interaction_Base`'s `Widget` component, but that widget is load-bearing twice over: `AC_Interaction` casts to it to drive the hold ring, AND `BP_Attachment_Pickup`'s own `Cast To UI_Interaction` gates the exec chain reaching `ShowForAttachment` — swap the class and the working stat panel dies. Reparenting WBP_PickupFocus to `UI_Interaction_C` is a UMG dead end (child tree can't carry inherited widget names — rename returns 'name already in use'). Open questions: does the V5 card REPLACE the kit card for attachments (and what owns the hold ring), or sit beside it on a second WidgetComponent; and does it stay world-anchored or move into the HUD? Also: the swap-off path spawns the BASE `BP_Attachment_Pickup_C` with only Slot/Value and an empty `ItemName`, so that case needs `GetAttachmentDisplayName(Weapon, KitSlot, Option)` exposed as a BlueprintPure static on `UAttachmentStatPreviewWidget` (the resolver exists but is anon-namespace static). The ICON has no data source at all — needs art, not code. **HUD V6 (05/08) — CLOSES that open question and ships four approved changes.** Director supplied `Codex/2026-08-04/im/outputs/final-hud-assets-simple`; 34 1x textures imported to `/Game/UI/HUD/ExtractionHUD/V6/` (⚠️ `import_texture`'s compression/lod_group opts silently no-op AGAIN — forced afterwards via `set()`; note V4/V5 were imported at **2x** so their brushes use half-size, V6 is 1x and brush size == source size). ⚠️ **Where the ChatGPT-written spec text and the preview PNGs disagree, the numeric spec won** (accent 200px@1566 not ~275@1510, active keycap centre 1665 not ~1630, rifle native 235x65 not upscaled) — widening the accent needs new art, not a stretch. (1) **Dual-weapon HUD:** `WBP_EquipmentRail` restructured from pure box-layout onto a CanvasPanel (no C++ BindWidget constrains any name in it) — root 695x137→698x200, HUDRoot slot Right/Bottom→698/200 with Left/Top unchanged, landing utility at X1190 and rail top at Y857. Active 422x101@1466,920 + stowed 300x66@1588,857, shared right edge 1888, 3px overlap, amber accent ZOrder above the tray so the overlap can't clip it. Weapon silhouettes use ScaleBox **ScaleToFit/DownOnly** because the stowed 146x52 box is smaller than both source arts and the pistol (150x72) overflows the active 246x70 box by 2px. Rail API single-slot→pair: `SetActiveWeapon/SetStowedWeapon(Icon,Loaded,Reserve,SlotNumber)` + `ClearStowedReadout`; `SetAmmo`/`RefreshWeapon`/`ClearAmmoReadout` kept for the active row since `OnAmmoChangedBP` still uses them. ⚠️ **`OnStowedWeaponChangedBP` had an event node with EVERY PIN UNCONNECTED** — the holstered row was dead BP-side too, not just unbuilt; built from nothing. Keycap digits are now live text from SlotNumber (deleted the 4 `RefreshKeyHints` nodes that wrote `WeaponKeyText` — they rendered "D-Pad Left" because kit `IA_PrimarySlot` has no keyboard binding). `Use Grouping` defaults TRUE on spawned `To Text (Integer)` — forced false or 1000+ reserve renders "1,000". (2) **Cyan ping:** preview 09 is authoritative; previews 02/03/04 still show the OLD orange ring and are STALE for ping. `Ring`→hover brackets 48x48, `MarkerIcon`→placed locator 24x24 (`DiaBox` 36→24), new `ConfirmPulse` 64x64 + a 180ms one-shot anim whose final keyframe is Collapsed AND opacity 0 (two independent hides), new 1px `LeaderLine` driven from `LeaderLineOffset`. ⚠️ Had to set the off-screen branch's `Set Render Scale(DiaBox)` 0.65→1.0 — that shrink existed only to tame the old 36px diamond and would render the new locator at 15.6px. Locator 24 units vs `WBP_WorldObjectiveMarker`'s 48 (which C++ scales 0.65–1.0 over 8–60m, so 24-vs-48 near, 24-vs-31 far). `T_Marker_PingRing`/`PingDiamond` now at ZERO referencers. Ping `Event Tick` was previously unconnected and now drives objective-suppression + leader line (per-frame values; `OnPingMarkerUpdated` is epsilon-deduped so Tick was the only correct driver). (3) **Pickup toasts:** new `UPickupToastStackWidget`/`UPickupToastRowWidget` C++ + `WBP_PickupToastStack`/`WBP_PickupToastRow`. ⚠️⚠️ **`UPanelWidget::ShiftChild` CANNOT reorder a VerticalBox at runtime** — it only reorders UMG's `Slots` array and calls `InvalidateLayoutAndVolatility`; the live `SVerticalBox` is only mutated by OnSlotAdded/OnSlotRemoved and `UVerticalBoxSlot::BuildSlot` always appends, so the Slots→Slate order mapping happens once in `RebuildWidget` and every other engine caller of ShiftChild is `#if WITH_EDITOR`. Container is a **UCanvasPanel** and C++ writes each row's **CanvasPanelSlot position**, NOT `SetRenderTranslation` — the row root's render transform is reserved for the designer's 120ms enter / 180ms exit animation, which would otherwise overwrite a C++ translation and hold its last keyframe, piling every toast onto one line (same hazard `UObjectiveMarkerWidget::bDriveRenderTransform` documents). ⚠️ Rows position at **negative Y from the newest**, so the container mounts at the NEWEST row's spec position (1468,787 size 420x58) and older rows overflow UPWARD — clipping must be `Inherit` on the whole ancestor chain or toasts 2 and 3 vanish silently. Rows are pooled; retirement reports back through `NotifyRowRetired` (re-entrancy latched) or an expired row leaves a permanent hole. `FOnLootGranted` widened to carry `EEnemyWeaponAnimType` so the toast picks a per-pool ammo icon (LMG+Shotgun→rifle icon/label); keycards pass `NAME_None` so two different cards can't merge. ⚠️ `WBP_LootToast` has **ZERO referencers project-wide** and the only other `OnLootGrantedBP` consumer is the hidden EHB module — so loot pickups rendered NOTHING before this. (4) **Icons from one source:** `UAttachmentIconSet` DataAsset at `V6/DA_AttachmentIcons`, 9 rows keyed on the **RAW KIT byte PAIR** — option byte meaning is PER SLOT (byte 1 = Holosight under slot 0, Laser under slot 1, Vertical grip under slot 3), so a lookup keyed on the option byte alone silently returns another slot's icon. Kit enums: `ENUM_AttachmentSlot` 0 Sight/1 Laser/2 Muzzle/3 Grip/4 Handguard/5 Barrel; options in `ENUM_Sights`/`Laser`/`Muzzle`/`LeftHand`/`Handguards`. ⚠️ NeoStack's `configure_at(arrayProp, idx, …)` is **1-BASED** — a 0-based pass wrote every row one slot off with no error. Focus card reads `GetFocusedAttachmentIcon()`/`GetFocusedAttachmentName()` off `GetAttachmentStatPreview()` via a **property binding, not an imperative read**: the bridge's prompt path and the kit's `Interface_ToggleFocus` path are independent, so a sampled read races and the bridge's dedup then never retries. Kit toast splice goes on `BP_Attachment_Pickup.Finish → ApplyItemData` (once per acquisition, `Slot`/`Value` in scope, upstream of all three `Destroy Actor(Self)` paths); the unconnected `Branch(1C965D0B).True` is the BLOCKED reloading/DBNO path, NOT success. Cover Me (protective fire, command-menu row 4) and Take Cover (order to enter cover, ping prompt) are separate icons with one referencer each — never interchange. `UWeaponComponent::BroadcastActiveWeaponChanged` gained `bForce` and `ReplaceSlotWeapon` now forces it: replacing the NON-HELD slot never broadcast, so the newly-permanent holstered panel kept showing the destroyed gun. Focus card is now MOUNTED and DRIVEN: `WBP_PickupFocus` sits in `WBP_HUDRoot.RootCanvas` (centre-anchored, 420x88, ZOrder 30) and `WBP_PickupToastStack` bottom-right at ZOrder 20, and the previously-unwired C++ event `OnPickupFocusChangedBP` now has a bridge node feeding a new `WBP_HUDRoot.SetPickupFocus` (stores `PickupFocusActive`, pushes `SetPickup`, calls `RefreshPromptPanels`). ⚠️ The precedence chain grew a fourth tier — Revive > Companion command > **Pickup focus** > Interact: `RefreshPromptPanels` now collapses the focus card unconditionally at entry and only re-shows it (collapsing ContextAction) on the no-companion-command branch, so the two ContextAction drivers still cannot both win. **First playtest defects fixed (05/08).** (a) ⚠️ **Pinging an objective rendered NOTHING.** A breach door carries an objective marker, and `UPingMarkerWidget` hid ALL its own visuals within `ObjectivePingRadius` of one — on the rule that the objective’s `OnObjectivePingFlash` is the feedback instead, a BIE **no widget implemented**. Worse: `UCompanionCommandComponent::IssuePing` has NO path that commits with `PendingCommand == None` (every non-match calls `ClearPending()` and returns), so a "plain location ping" DOES NOT EXIST in this game and the rule would have swallowed EVERY ping near ANY objective. Suppression now gates on one predicate `IsPlainPlacedPing()` (`bLastPlaced && PingCommand == None`), unreachable today; `bIsObjectivePing`, `ObjectivePingRadius` and the narrow-enter/wide-exit hysteresis are RETAINED and documented as inert (WBP_PingMarker binds the flag) rather than deleted, so a command-less ping would switch them back on. `FlashMarkerNearWorldLocation` stays unconditional, and `WBP_WorldObjectiveMarker` now implements `OnObjectivePingFlash` — a 300ms one-shot `PingFlash` anim (cyan tint on MarkerIcon/ChevIcon/PointerIcon + 1.18 scale punch on an inner `Group`), deliberately avoiding the widget-root transform/opacity that C++ `ApplyDistanceVisuals` writes every tick and the ChevBox/IconBox scale+opacity the state graph writes. ⚠️ A breach ping on an objective now runs the avoidance path suppression used to short-circuit, so the locator sits up to `ObjectiveClearanceRadius` (48px) off it with a leader line. (b) ⚠️ **The attachment focus card on screen is the KIT’s `UI_Interaction`, NOT `WBP_PickupFocus`** — restyled by this project so it reads as native. Identify it by keycap on the RIGHT with the hold ring `WB_RoundBar` inside the keycap overlay; `WBP_ContextAction`/`WBP_PingPrompt` put the keycap LEFT with chevrons right. Its `CategoryIcon` carried a HARDCODED V5 `T_Icon_Attachment` brush with zero bindings and no setter — that, not the C++, is why every attachment showed one glyph. Fixed by ticking `Is Variable` on `CategoryIcon` (additive, and the kit asset was already project-modified) and splicing `FindIcon(Slot, Value)` off `DA_AttachmentIcons` into `Interface_ToggleFocus` between `SetText` and the PC cast, with a `FallbackIcon` for byte pairs with no row (slot 5 Barrel). Also assigned the MISSING `AttachmentIcons` DataAsset on `WBP_AttachmentStatPreview` — it was `None`, so `GetFocusedAttachmentIcon()` could only ever return null (the toast stack had it, which is why notifications were right and focus was not). ⚠️ **`WBP_PickupFocus` can NEVER show for attachment pickups.** `AExtractionPlayer::RefreshPromptState` only sets `EHudPromptKind::Interact` for actors implementing the C++ `IWorldInteractable`, and `BP_Attachment_Pickup` (parent `BP_Interaction_Base_C` → `Actor`) implements only the kit’s BP `INT_Interaction`. The kit runs a wholly separate `AC_Interaction` tick-trace stack — there are even two distinct `IA_Interact` assets. A BP-callable bridge channel to feed the card was written and REVERTED unshipped (director’s call: the kit card with correct icons wins). (c) **Companion world vitals rebuilt to preview 06:** 180x34 → 180x42, armor 141x6 cyan over health 141x8 green, both track + left-cropping clip, pulse glyph added, no numerals; `SetHealth` multipliers 136 → 141. `OnShieldUpdated` was already fully wired and is bound TWICE — the widget graph’s `Assign On Shield Changed` AND `ACompanionCharacter::HandleShieldChangedForWidget` (ProcessEvent by name). Idempotent, but the C++ comment claiming the graph cannot bind it is STALE. `MinScale` 0.5 → 0.7 (design floor; not overridden in BP). ⚠️ `DrawSize` is an INSTANCE OVERRIDE on BP_Companion’s `HealthWidget` and BEATS the constructor — it was stale at 180x34. Set to 180x42 via NeoStack’s **two-argument** form `set("HealthWidget", "DrawSize", ...)`; the dot-path form fails on native component templates. It still does not track the constructor and will go stale again if the widget resizes. **Second playtest pass (05/08) — four asks, ZERO C++ between them; all four were wiring gaps, not missing features.** (1) **Hold bar said REVIVING while rescuing the extractee.** The rescue is `EHudPromptKind::Interact`, NOT `Revive` — `ReviveCandidate` is only set by `FindReviveTarget()`, which requires `GetIsDBNO()`, and `AExtracteeCompanion` is `bCaptive`, never DBNO; it supplies its 5s hold through `GetWorldInteractHoldSeconds` → `RescueHoldSeconds`. `WBP_HUDRoot` already stores `LastPromptKind`/`LastPromptText` before any hold begins (the 0.1s hover scan guarantees it), so this needed no new channel: new `WBP_ReviveProgress.SetLabel(FText)` + a branch in `SetPromptHoldStarted`. ⚠️ A naive `Revive→REVIVING / Interact→RESCUING` split is WRONG — `AInteractionVolume` also resolves as `Interact` and `IV_CallExtraction` (DemoMap) has `HoldSeconds` 2.0, so it was silently mislabelled "REVIVING" already. Rule shipped: Revive→`REVIVING`; Interact + text == "Rescue" (case-insensitive)→`RESCUING`; Interact otherwise→`Text to Upper(LastPromptText)` (so the console reads `CALL FOR EXTRACTION`); empty→`REVIVING`. Rule 2 is a string match against the designer-editable `AExtracteeCompanion::RescuePrompt` and degrades gracefully to `RESCUE` if edited. `RevivingText` is authored uppercase with `TextTransformPolicy=None`, so only rule 3 uppercases. (2) **Extractee VIP had the OLD health plate.** ⚠️ `BP_ExtracteeCompanion` had the pitfall on BOTH properties — `HealthWidgetClass` AND the `HealthWidget` component instance’s own `WidgetClass` were `WBP_CompanionHealth_C`; setting only one looks right and does nothing, because `UWidgetComponent::BeginPlay` calls `InitWidget()` from the component property before `ACompanionCharacter::BeginPlay` reaches `SetWidgetClass`. Both now `WBP_CompanionWorldHealth_C`. `AExtracteeCompanion` inherits `ACompanionCharacter`’s ENTIRE health stack (same `UHealthComponent`, same three delegates, 100/50 health/shield), so the widget’s `Initialize`/`BindCompanion` bound with no changes and it needed no `DrawSize` override. NOTE the OTHER extractee, `AExtracteeCharacter` (the civilian), is `ACharacter` with NO health component at all — `TakeDamage` hard-returns 0, unkillable by design — so it can never carry this plate without new state. (3) **Vitals plates now always visible.** `RefreshVisibility` used to `SetVisibility(Collapsed)` whenever `bIsDowned OR bTargeted OR bRecentlyDamaged OR HealthPct < 1` was false, so a healthy companion had no plate at all. Now permanently `HitTestInvisible`, with that same OR-chain picking a `RenderOpacity` target instead: `RestOpacity` 0.3 → `BoostedOpacity` 1.0, lerped on `Event Tick` (previously a dead disconnected node) at `OpacityFadeSpeed` 8.0, all three EditAnywhere. Two-gate fade lands EXACTLY on target then short-circuits, so it costs nothing at rest. `Event Construct` seeds the opacity so a spawning plate starts dim. ⚠️ Opacity is WIDGET-side only by design: `UOverheadWidgetComponent` writes ONLY `SetRenderScale` (distance falloff + occlusion collapse) and its own doc comment says it stays off opacity so widgets can own it — do not start writing `RenderScale` from the widget. Occlusion hide still collapses scale behind geometry. (4) **"Follow your partner" marker was static at the top of the stairs.** Actor-following ALREADY WORKS — `FObjectiveMarker::ResolveLocation()` re-samples `TargetActor`’s bounds base every call and `UObjectiveMarkerWidget::NativeTick` calls it every frame. `OS_02_FollowCompanionDownstairs` simply had `MarkerTarget` unset, and its `RouteCompleted` condition has no case in `AObjectiveStep::ResolveMarkerTarget()`’s switch, so it fell through to the step actor’s own placed transform. Wired `MarkerTarget` → the level-placed `BP_Companion`; `MarkerHeightAboveBase` is 170 (head height). ⚠️ Deliberately did NOT add a `RouteCompleted` case — other steps use that condition and may want the static location; `MarkerTarget` is the documented per-instance escape hatch. The HUD distance readout reads the same resolver, so both move together. Pending PIE: every one of the 10 validation scenarios — nothing here is PIE-verified.
- [x] [Stretch] **Hitmarkers + damage numbers** (Battlefield-style HUD juice). C++-driven off a single player-damage feedback event on the weapon hitscan pass (`AExtractionPlayerController::OnDamageDealt`); `UHitmarkerWidget` X-marker (single white ticks = body hit, doubled ticks = headshot, coral-red + expand = kill, doubled coral = headshot kill; per-hit impact pop, hidden until first hit) + `UDamageNumberWidget`/`UDamageNumberEntryWidget` per-hit stacking numbers in a fixed column left of the crosshair (Saira Condensed Bold, white body / dark-red headshot); headshot hits scale the enemy blood burst (`HeadshotBloodScale`). WBP_Hitmarker/WBP_DamageNumbers/WBP_DamageNumberEntry wired on BP_ExtractionPlayerController; sounds handled separately by the user. PIE-iterated + user-approved.
- [ ] [Stretch] **UI/UX.** Main menu (marketplace-bought, shooter-themed), class-creation / loadout editor (COD/BF style — set weapons + attachments pre-mission), and a minimap.
- [~] [Stretch] **Full polish.** Enemy + companion voice shouts (companion with a bit of personality/depth), weapon FX + reload sounds, music (menu + in-game for combat / extraction). — **Voice shouts DONE + playtested (bark v2):** subtitle feed replaced with real 3D VO — 225 AI-generated lines (138 companion + 1 pistol-handoff / 70 enemy / 17 extractee VIP, 24/07) played spatially from the speaker with occlusion (muffled through walls) and distance falloff to the 40 m gate; UBarkSubsystem one-voice-at-a-time arbitration (priority telegraphs interrupt, ambient only in lulls, per-speaker cooldowns); companion speaks 36 wired bark types (contact/direction/archetype callouts, full down/revive relationship set, command acks, stealth whispers, reload/kill confirms) via ECompanionBarkType + DA_Barks_Companion; enemy archetype voice sets (DA_Barks_Grunt full baseline + sparse specialist sets Officer/Rusher/Heavy/Sniper/Grenadier incl. new Rally/Rush/Taunt/Overwatch/CallingContact barks); companion overhead widget shows a shield bar above the health bar and the companion health-regens shield-style (4/s after 8s); player-revives-companion montages switched OFF (bPlayPlayerReviveMontages, hold mechanics unchanged); **world-audio pass BUILT + in-engine wired, awaiting PIE (WARFARE_SOUNDS):** data-driven bank (`USurfaceAudioBank` DA + `UGameAudioSubsystem`, Project Settings soft ref) with surface-typed bullet impacts (phys-material driven, all shooters via the shared `AWeaponBase` hitscan, flesh crack+thump per victim), distance-based surface-aware footsteps/land/sprint-rattle for player+enemies+companion (extended `FootstepNoiseComponent`, audio cadence separate from AI-noise cadence), enemy-shot flyby cracks (near-miss hook), shell tinkle, jump/crouch/switch/throw foley, door open + 3 breach-type accents, pickup sounds, DBNO heartbeat loop + duck mix (SMix_DBNO), kit breathing/footsteps/jump/land/select removed from BP_ExtractionCharacter, takedown draw/swing/kill notifies, room-tone bed + spot hums in level; 42 randomized SoundCues + SoundClass mix hierarchy under /Game/Core/Audio; phys materials bulk-assigned to 141 level materials; music pending. **Audio stability + feel round PIE-iterated + user-approved (07-23):** project-wide sound concurrency caps (per-cue gunfire/impacts/footsteps groups + a global default in AudioSettings) fixing the total-audio dropout under heavy load; anim-synced footsteps via runtime foot-plant tracking (distance cadence kept as backstop) + crouch-stride step distance + quiet crouch gear rattle; global ambient reverb activated from the audio bank + distance low-pass/occlusion depth on enemy+companion VO; enemy ManDown barks gated on witnessing the death (earshot or LOS); companion "wait up" debounced to sustained catch-up; loot pickup equipment layer; headshot rebuilt around a bone-crunch layer. DemoMap ambience bed deferred to integration. **In-game music SHIPPED + user-approved (07-23):** `UMusicSubsystem` always-on adaptive backing track (Explore/Suspicion/Combat/Wave states driven by `UEnemyDirectorSubsystem` searching/combat counts + wave events + mission phase), continuous quiet stealth bed with tension/combat crossfades (no silence gaps — reworked after playtest feedback), combat fades down cleanly on fight-end (no jarring all-clear stinger), Peak-tension percussion overlay, per-mission-phase track palettes (Infiltration/Objective/Extraction finale), DA_MusicBank populated from the purchased STC Music Pack, DBNO mix ducks music. Menu music (Music_Main_Theme available in-pack) still pending. **Audio feedback round BUILT + wired, awaiting PIE (07-24):** stealth bed audibility fixed via per-cue loudness-matching (all 4 explore cues perceptually level; Loop8 was 7 dB under Loop3 — the "empty 5s" was faintness, waves have no silence) + EntryFadeSeconds 0 (truly instant at level start); combat wind-down now ~3s after last combat signal (CombatHoldSeconds 8→3) so Loop1_Combat_End_Sting_2 lands on the moment; enemy shouts VolumeMultiplier 1.5→2.0 across all 6 sets and all 28 pitch-shifted `_B`/`_C` distorted takes pruned from DA_Barks_Grunt; companion chatter densified (ambient attempt interval 45s→20s as BT-editable `AmbientAttemptIntervalSeconds`, bark-channel ambient lull 10s→6s, per-type cooldowns tuned) + 55 new AI-generated lines (45 wired into DA_Barks_Companion across 12 types incl. banter/idle/reassurance depth, 10 reserved as 5 scripted trigger pools); NEW `ABarkTriggerVolume` level-placeable scripted-dialogue box (multi-line sequences through the one-voice bark channel, DBNO/combat/busy-gated, once-only default) with a LobbyEntry test box placed ahead of the DemoMap PlayerStart. **Bark register pass (bark v3):** 52 clips regenerated to an operator-brevity register — 19 companion types replaced in DA_Barks_Companion (film-quip lines cut: "That's how it's done", "Remind me why we took this job", "Doesn't feel right, this quiet"), 27 superseded takes left unreferenced on disk; the 4 stealth types + Breaching's Quiet variant are now genuinely whispered (eleven_v3 [whispers] tag, rest on multilingual_v2); EnemyArchetype's Grenadier variant dropped by design decision (grenadier sightings silent); VIP rescue exchange rewritten and fully wired — CompanionRescueStartLine was unassigned so beat 1 had never had audio. Script of record: agent_docs/bark_script.md. FBarkVariant::Line documented as a retired subtitle field, kept as the written record + future hook. **Combat wind-down rework (07-26, awaiting PIE):** fixes combat music dropping to stealth during a lull. Root cause was the hold gating on `UEnemyDirectorSubsystem::GetLastCombatReportTime()`, which stamps combat *entry* only — any fight longer than the hold had it already expired, so the count hitting zero wound down on the next poll; `LastCombatSignalTime` now stamped in `Poll` whenever the live combat count is non-zero and the gate takes the later of the two, making `CombatHoldSeconds` actually load-bearing (the 8→3 tuning of the 07-24 round was compensating for a hold that never fired). Relief no longer stops the bed — `DuckOrStopForRelief` rides the fight track down to `MusicVolume * ReliefDuckVolume` and leaves it looping, then `TryReliefHandoff` slow-crossfades it into the phase's explore bed over `ReliefHandoffFadeSeconds`; re-ignition anywhere in that window reclaims the same component back to full via `TryResumeDuckedTrack` (from `CurrentTrack` while ducked, or from `OutgoingTrack` mid-handoff via `bOutgoingFightBed`) with no bar-1 restart and no repeated `StingerCombatIn`, declining only when a mission-phase flip made the ducked bed the wrong palette. New DA tunables `ReliefDuckVolume` 0.3 / `ReliefDuckFadeSeconds` 6 / `ReliefHandoffFadeSeconds` 10; DA_MusicBank `CombatHoldSeconds` 3→10, `ReliefSeconds` 5→20. All-clear stinger moved to duck-settle via a polled `PendingAllClearTime` (note: `StingerAllClear` is unassigned in the bank, so it currently plays nothing). Shape off the last combatant dying: 10 s full → 6 s duck to 30% → quiet hold → 10 s crossfade, stealth established ~40 s. Reviewed clean (0 critical / 0 warnings). Known gaps: a searcher appearing inside the 10 s handoff hard-cuts the outgoing bed (needs a third component slot); bank has `ExploreSilenceMin/MaxSeconds` both saved at 0 and `EntryFadeSeconds` 0. Enemy `LostContactGrace` is 45 s on all eight archetypes (none override), so an LOS break holds combat awareness far longer than the music hold — the dominant wind-down trigger is the player killing the last combatant. **EnemyDirection VO gated down (02/08, user call):** variant selection treats an untagged `Context` as eligible for EVERY bearing, so five directional lines fired on the wrong side — "Above us! Balcony rail!", "Watch your left, moving fast", "Right side, behind the pillar!", "Behind! Check behind!" (all untagged) plus the correctly-tagged "Up high, the balcony." which the user also wanted out. All five parked by setting `Context=DisabledForNow` in `DA_Barks_Companion` (reversible: clear the field). Live pool is now Left / Right / Front only. Consequence: an elevated target gets NO direction callout — the only High-tagged take was one of the disabled balcony lines, so a correct high callout needs a new take recorded, not a re-enable.
- [~] [Core] **Data-driven objective system (`AObjectiveStep`).** Replaces the hardcoded `ALevelObjectiveFlow`, whose 13-step chain lives in C++ enums so every new beat is a code change across ~7 sites. One placed actor per beat, chained by `NextStep`: condition dropdown (reach / keycard / enemies dead / door opened / container looted / route completed / interacted / extractee rescued / wave completed / manual), declarative side effects (set mission phase, start director wave, unlock gate, set companion mode, complete level, activate actor, set extractee rescuable, command companion route) firing on activate or complete, and checkpoints whose resume replays each earlier beat's world-state derived from its own condition. C++ COMPLETE through 4 fix rounds + 3 review passes (round 3: 0 critical / 3 warnings, all since fixed), **build verified green 28/07**. **DemoMap MIGRATED 28/07** — 13 placed step actors (12-beat main chain + the optional-supplies side chain) replace `LevelObjectiveFlow_DemoMap`, which was deleted in the same pass along with the now-redundant downstairs `CompanionRouteTrigger`. Wiring independently verified against the captured legacy values: all 29 references, both checkpoints (`FindOfficeKeycard`, `ReachExtractionTarget`), and the `Room2Defence` wave request verbatim (6 squads, cadence 8.0, `HeavyPush` guaranteed at squads 3 and 6). Short PIE showed one active objective and no audit warnings. Authoring spec: `agent_docs/objective_step_migration_plan.md`; captured legacy state: `agent_docs/objective_flow_migration_source.md`. **NOT yet playtested end-to-end** — the full chain and both checkpoint restarts still need a real run. **Checkpoint loadout persistence SHIPPED 30/07 (build green, PIE unverified):** a checkpoint restart used to respawn the player on `DefaultWeaponClass` with default ammo and no attachments, because the character BP's `Load` set the slot records but never called `ReplaceSlotWeapon`, so the actual weapon actors stayed as spawned. Now `UExtractionGameInstance` holds an `FCheckpointLoadoutSnapshot` keyed by level name (same lifetime as the objective checkpoint, cleared by `ClearCheckpoint` on level completion so a replay resets), captured at BOTH record sites (`LevelObjectiveFlow` + `AObjectiveStep::RecordCheckpoint`) and replayed next-tick on respawn. Restores both gun slots (class/mag/reserve), all SIX attachment bytes incl. the sniper-only Barrel the C++ stat layer never carried, throwable, melee, stim count, and the held slot. ⚠️ Three traps found in review, all fixed, do not reintroduce: the resume path re-fires `RecordCheckpoint` during `ActivateInternal` on the restart frame and would capture the *default* loadout over the good snapshot (guarded by a `bLoadoutRestoreSettled` latch that only settles on `VerifyLoadoutRestore`'s terminal paths, plus a cached snapshot copy); the BP slot record MUST be written before `ReplaceSlotWeapon` or the `OnWeaponEquipped` guard wipes `ResolvedAttachments`; and the kit's `SwapWeapon` event is DEAD (unconnected exec pin) so the held slot is re-selected from C++ via the new `UWeaponComponent::ActivateSlot`.
- [ ] [Core] **Integrate the full level.** Both halves end-to-end, full objective flow, a difficulty-consistency pass (no scaling across the level).
- [ ] [Core] **Final verify.** Confirm the whole list is present and working; capture final demo footage.

### After W7 — 10–19/08 sporadic buffer (NY → return; hard deadline 19/8)
- [ ] **Contingency** — absorb any slipped Stretch (in-game music, 2nd-half dressing, UI polish).
- [ ] **Dissertation paper** — write and finalise from captured notes + footage.
- [ ] **Gameplay demo** — cut and finalise from captured footage.
- [ ] **Final bug-fix / stabilise** pass to the 19/08 deadline.
