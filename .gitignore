# ============================================================================
# Unreal Engine 5 .gitignore
# ============================================================================

# ----------------------------------------------------------------------------
# Build outputs
# ----------------------------------------------------------------------------
Extraction/Binaries/
Extraction/Intermediate/
Extraction/DerivedDataCache/
Extraction/Build/

# Packaged builds (large local artifacts — never tracked)
Extraction/Package/
Extraction/PAckage/

# ----------------------------------------------------------------------------
# Saved / local data
# ----------------------------------------------------------------------------
Extraction/Saved/

# ----------------------------------------------------------------------------
# IDE / editor project files (regenerated by UBT)
# ----------------------------------------------------------------------------
*.sln
*.xcodeproj
*.xcworkspace
.vs/
.idea/
.vscode/
*.VC.db
*.VC.opendb
*.suo
*.opensdf
*.sdf

# ----------------------------------------------------------------------------
# OS junk
# ----------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

# ----------------------------------------------------------------------------
# Built map data
# ----------------------------------------------------------------------------
*_BuiltData.uasset

# ----------------------------------------------------------------------------
# Local / per-user config (not project defaults)
# ----------------------------------------------------------------------------
Extraction/Config/DefaultEditorPerProjectUserSettings.ini

# ----------------------------------------------------------------------------
# Claude Code — shared config & docs are committed for cross-machine parity;
# only transient agent-team worktrees stay local
# ----------------------------------------------------------------------------
.claude/worktrees/
.omc/
Extraction/Config/DefaultAgentIntegrationKit.ini

# ----------------------------------------------------------------------------
# Third-party plugins
# NeoStackAI / AgentIntegrationKit_backup stay local (untracked).
# VibeUE + UnrealClaude are vendored — their own nested .gitignore files
# exclude Binaries/Intermediate; UnrealClaude keeps its mcp-bridge node_modules.
# ----------------------------------------------------------------------------
Extraction/Plugins/*
!Extraction/Plugins/VibeUE
!Extraction/Plugins/UnrealClaude
# Added by code-review-graph
.code-review-graph/

# ----------------------------------------------------------------------------
# Local diagnostic build logs (not for tracking)
# ----------------------------------------------------------------------------
build_*.log

# ----------------------------------------------------------------------------
# Oversized kit assets (over GitHub's 100MB limit, no LFS configured)
# ----------------------------------------------------------------------------
Extraction/Content/ProceduralFPSKIT/Demo/Lyra_Content/Effects/Textures/Flipbooks/T_SmokeSwirl_3_Flipbook_CHANNELPACK.uasset
/Extraction/SSs
